---
phase: 18-live-machine-validation-system-health-and-apps
plan: 03
type: execute
wave: 3
depends_on: [18-01, 18-02]
files_modified:
  - collectors/windows/hardware.py
  - collectors/windows/apps.py
  - health_checks.py
  - renderer/__init__.py
  - tests/test_health_checks.py
  - tests/test_app_collector.py
  - tests/test_renderer.py
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [VALID-01, VALID-03, VALID-05]
user_setup: []

must_haves:
  truths:
    - "If all SCs are PASS or DEFERRED, no code files are modified — only STATE.md, REQUIREMENTS.md, and ROADMAP.md are updated"
    - "If any SC FAILS, a targeted code fix is applied to the relevant module and a regression test is added before phase close"
    - "`.planning/STATE.md` frontmatter shows `status: phase-complete` and `completed_phases: 3`"
    - "`.planning/REQUIREMENTS.md` traceability table shows VALID-01, VALID-03, VALID-05 as `complete` with `18-01, 18-02, 18-03` plan references"
    - "`.planning/REQUIREMENTS.md` checkboxes for VALID-01, VALID-03, VALID-05 are ticked (`[x]`)"
    - "ROADMAP.md Phase 18 checkbox is ticked and shows `3/3 plans`"
    - "Full test suite passes with no regressions"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Phase pointer advanced to Phase 19; status phase-complete; completed_phases incremented to 3"
      contains: "phase-complete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VALID-01/VALID-03/VALID-05 marked complete in checkboxes and traceability table"
      contains: "VALID-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 18 checkbox ticked; plan count updated to 3/3"
      contains: "[x] **Phase 18"
  key_links:
    - from: "18-VALIDATION-RESULTS.md Summary table"
      to: "Task 1 decision branch"
      via: "Task 1 reads Summary table dispositions before any code change or phase close"
      pattern: "PASS|FAIL|DEFERRED"
    - from: "Conditional code fix (Task 2)"
      to: "Regression test"
      via: "Every code change paired with a test (mirrors D-09/D-13 discipline from Phase 17)"
      pattern: "def test_"
---

<objective>
Apply conditional patches based on Edgar's findings in `18-VALIDATION-RESULTS.md` and atomically close Phase 18.

Purpose: Plan 18-01 created the validation artifact skeleton. Plan 18-02 captured Edgar's findings. Plan 18-03 is the decision step — read the Summary table in `18-VALIDATION-RESULTS.md` and either (a) apply targeted code fixes per D-09 if any SC shows FAIL, or (b) leave the code unchanged if all SCs are PASS or DEFERRED. Then close the phase: advance STATE.md, mark VALID-01/VALID-03/VALID-05 complete in REQUIREMENTS.md, tick the ROADMAP checkbox, and run the full test suite.

This plan is `autonomous: false` because the branching depends on Edgar's findings (which Plan 18-02's resume signal already revealed). The executor reads the resume signal plus the file and applies the conditional logic deterministically — no further human decision is needed.

Output: A defensible code state matching what Edgar observed; clean STATE.md, REQUIREMENTS.md, and ROADMAP.md; closed VALID-01, VALID-03, and VALID-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-live-machine-validation-system-health-and-apps/18-CONTEXT.md
@.planning/phases/18-live-machine-validation-system-health-and-apps/18-PATTERNS.md
@.planning/phases/18-live-machine-validation-system-health-and-apps/18-VALIDATION-RESULTS.md
@.planning/phases/18-live-machine-validation-system-health-and-apps/18-01-SUMMARY.md
@.planning/phases/18-live-machine-validation-system-health-and-apps/18-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Current production code paths Phase 18-03 may touch if a SC fails. -->
<!-- Read_first for Task 2 only; Task 1 (decision) and Task 3 (phase close) do not touch these. -->

From collectors/windows/hardware.py:
  collect_pending_updates() — WUA COM call; returns CollectionResult(value, error).
    Returns None gracefully when _WIN32COM_AVAILABLE is False or privilege insufficient.
    SC1/SC3 behavior is implemented here. If SC1 or SC3 FAIL, the fix is in this function.

From collectors/windows/apps.py:
  _search_uninstall_keys(keywords) — enumerates all 4 hives (HKLM, HKLM\Wow6432Node, HKCU, HKCU\Wow6432Node).
    NinjaOne keywords: ["NinjaRMMAgent", "NinjaRMM", "NinjaOne Agent"]
    CrowdStrike keywords: ["CrowdStrike Windows Sensor", "CrowdStrike Sensor Platform"] + service state read
    M365 single-suite collapse logic
    Company Portal: MSIX + MDM Enrollment registry check (no elevation needed)
  If SC4 FAILs, the fix targets the relevant keyword list or detection logic here.

From health_checks.py:
  UPTIME_WARN_DAYS = 7 (lines 132-161)
  UPTIME_STALE_DAYS = 30 (lines 132-161)
  check_uptime_warning() — badge logic for UPTIME_WARN and UPTIME_STALE.
  Pre-validated by Justin (SC2 PASS); extremely unlikely to require a fix.

From renderer/__init__.py:
  render_html() — produces the HTML character sheet from AuditReport.
  If SC5 FAILs, the fix targets template rendering, Jinja2 context passing, or CSS loading.

CollectionResult envelope pattern (all collectors):
  All collectors return CollectionResult(value=..., error=...) — NEVER raise across layer boundary.
  A SC that surfaces as None / "N/A" (rather than a crash) means the error-handling envelope
  is working; the fix is in the data-gathering logic, not the error boundary.

Test coverage for each SC:
  SC1/SC3 (pending updates): tests/test_health_checks.py lines 182-205 (uptime badge tests)
  SC2 (uptime badges): tests/test_health_checks.py lines 182-205 — already PASS, no change expected
  SC4 (app detection): tests/test_app_collector.py lines 55-817 (NinjaOne, CrowdStrike, M365, Company Portal)
  SC5 (HTML render): tests/test_renderer.py
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Read `18-VALIDATION-RESULTS.md` Summary table and confirm disposition</name>
  <decision>Which SCs, if any, show FAIL and require a code fix? Which SCs are DEFERRED?</decision>
  <read_first>
    - .planning/phases/18-live-machine-validation-system-health-and-apps/18-VALIDATION-RESULTS.md (Summary table at the top — read this first)
    - .planning/phases/18-live-machine-validation-system-health-and-apps/18-02-SUMMARY.md (resume signal from Plan 18-02)
  </read_first>
  <context>
    Plan 18-02's resume signal already conveyed the dispositions, but Plan 18-03 re-reads `18-VALIDATION-RESULTS.md` directly to avoid drift between the signal and the file. Before touching any code or planning docs, confirm the disposition for each SC (SC1–SC5) and the specific failure symptom for any FAIL. The executor reads the Summary table and reports back to the user for confirmation before proceeding.

    This is a `checkpoint:decision` because the wrong branch produces silently wrong code (or a premature phase close with an unresolved FAIL). Explicit confirmation is better than guessing.
  </context>
  <options>
    <option id="option-no-changes">
      <name>All SCs are PASS or DEFERRED — no code changes needed</name>
      <pros>Edgar confirmed all behavior is correct (or any non-validated SC is documented as DEFERRED). Skip Task 2 entirely; proceed directly to Task 3 (phase close).</pros>
      <cons>None — this is the expected and simplest path (D-10: high confidence).</cons>
    </option>
    <option id="option-fix-sc1">
      <name>SC1 FAIL — uptime or pending updates not showing real values under Admin account</name>
      <pros>Targeted fix to `collect_pending_updates()` or `_collect_uptime()` in `collectors/windows/hardware.py`, plus regression test. Fix the specific symptom Edgar observed.</pros>
      <cons>Must preserve the CollectionResult(value, error) envelope — never raise across layer boundary.</cons>
    </option>
    <option id="option-fix-sc3">
      <name>SC3 FAIL — pending updates not showing "N/A" as standard user</name>
      <pros>Targeted fix to `collect_pending_updates()` privilege-degradation path in `collectors/windows/hardware.py`, plus regression test.</pros>
      <cons>Must ensure the fix does not break SC1 (Admin path) while fixing SC3 (non-admin path).</cons>
    </option>
    <option id="option-fix-sc4">
      <name>SC4 FAIL — app detection missing (NinjaOne, CrowdStrike, M365, or Company Portal)</name>
      <pros>Targeted fix to the relevant keyword list or detection logic in `collectors/windows/apps.py`, plus regression test. Apply only to the failing app(s) — do not modify working detections.</pros>
      <cons>ALWAYS use all 4 hives (HKLM, HKLM\Wow6432Node, HKCU, HKCU\Wow6432Node) per CLAUDE.md constraint — missing 32-bit entries is a silent bug.</cons>
    </option>
    <option id="option-fix-sc5">
      <name>SC5 FAIL — HTML character sheet not rendering correctly in real browser</name>
      <pros>Targeted fix to `renderer/__init__.py` render_html(), Jinja2 template, or CSS loading via importlib.resources, plus regression test.</pros>
      <cons>CSS is loaded via importlib.resources — do NOT switch to filesystem path loading (incompatible with PyInstaller --onedir packaging).</cons>
    </option>
    <option id="option-multiple-failures">
      <name>Multiple SCs FAIL — two or more SCs need fixes</name>
      <pros>Apply each SC's fix branch in sequence (SC1/SC3 first, then SC4, then SC5). Each code change paired with its own regression test.</pros>
      <cons>Every fix must be paired with a test (D-09 pairing discipline). After all fixes, Edgar must re-validate the previously-failing SCs before phase close.</cons>
    </option>
  </options>
  <how-to-verify>
    1. Read `18-VALIDATION-RESULTS.md` Summary table.
    2. For each SC, note the Result column value: PASS, FAIL, or DEFERRED.
    3. For any FAIL SC, scroll to that SC's section and read the `**Divergence notes:**` field for the exact symptom.
    4. For any DEFERRED SC, read the `**Deferred rationale:**` field to confirm it is documented.
    5. State the chosen option and the exact failure symptoms (if any) back to the user for confirmation.
  </how-to-verify>
  <acceptance_criteria>
    - The executor has read `18-VALIDATION-RESULTS.md` Summary table (not inferred from the resume signal alone)
    - The chosen option is stated with exact SC disposition values
    - For any FAIL, the `Divergence notes` symptom is quoted verbatim from the file
    - For any DEFERRED, the `Deferred rationale` is acknowledged
  </acceptance_criteria>
  <resume-signal>
    Type one of:
      - `option-no-changes` (skip Task 2; proceed to Task 3 phase close)
      - `option-fix-sc1: <exact symptom from Divergence notes>`
      - `option-fix-sc3: <exact symptom>`
      - `option-fix-sc4: <exact symptom per app — e.g. "NinjaOne not detected on CHI-IT-042">`
      - `option-fix-sc5: <exact symptom — e.g. "dark CSS not applied in Edge">`
      - `option-multiple-failures: <comma-separated list of failing SCs with symptoms>`
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: (Conditional) Apply targeted code fix per Task 1 decision + regression test</name>
  <files>collectors/windows/hardware.py, collectors/windows/apps.py, health_checks.py, renderer/__init__.py, tests/test_health_checks.py, tests/test_app_collector.py, tests/test_renderer.py</files>
  <read_first>
    - .planning/phases/18-live-machine-validation-system-health-and-apps/18-VALIDATION-RESULTS.md (failing SC's `Divergence notes` — copy the symptom verbatim; do not infer)
    - The failing module (read in full before editing — only the modules relevant to failing SCs):
        SC1/SC3 fail: collectors/windows/hardware.py
        SC4 fail: collectors/windows/apps.py
        SC5 fail: renderer/__init__.py
        SC2 fail (extremely unlikely): health_checks.py
    - The corresponding test file (read in full to understand existing test helpers before adding new tests):
        SC1/SC3: tests/test_health_checks.py
        SC4: tests/test_app_collector.py
        SC5: tests/test_renderer.py
    - .planning/phases/18-live-machine-validation-system-health-and-apps/18-CONTEXT.md code_context section (canonical code path references)
    - CLAUDE.md "Critical Technical Constraints" (NEVER Win32_Product, ALWAYS 4 hives, CollectionResult envelope, importlib.resources for CSS)
  </read_first>
  <behavior>
    - SC1/SC3 regression test: mock the WUA COM call; assert collect_pending_updates() returns a real value under mock-elevated context (SC1) and None under mock-standard context (SC3)
    - SC4 regression test: mock the registry hive containing the failing app's Uninstall key; assert the app is detected as installed (use existing _make_query_fn / _make_enum_fn patterns from tests/test_app_collector.py)
    - SC5 regression test: render a minimal AuditReport; assert the generated HTML contains the required structure markers
  </behavior>
  <action>
    SKIP THIS TASK ENTIRELY if Task 1's resume signal was `option-no-changes`. Verify with the user that all SCs are PASS or DEFERRED, then move directly to Task 3.

    Otherwise, for each failing SC named in Task 1's resume signal, apply the corresponding branch:

    ---

    **Branch SC1/SC3 — Uptime or pending updates misbehaving (collectors/windows/hardware.py):**

    1. Read `collectors/windows/hardware.py` in full before editing.
    2. Identify the exact failure in `collect_pending_updates()` based on the Divergence notes symptom from `18-VALIDATION-RESULTS.md`:
       - If uptime shows "N/A" under Admin (SC1): fix the `_collect_uptime()` psutil.boot_time() path.
       - If pending updates shows "N/A" under Admin (SC1): fix the WUA COM call path in `collect_pending_updates()`.
       - If pending updates does NOT show "N/A" for standard user (SC3): fix the privilege check that gates the WUA COM call.
    3. Apply the minimum targeted fix. Preserve the `CollectionResult(value, error)` return shape — never raise across layer boundary.
    4. In `tests/test_health_checks.py`, add a regression test that:
       - (SC1) Mocks the WUA COM call as available + mock-elevated; asserts collect_pending_updates() returns a non-None CollectionResult with a numeric value.
       - (SC3) Mocks _WIN32COM_AVAILABLE as False (or privilege check as failing); asserts collect_pending_updates() returns CollectionResult(value=None, error=...) without raising.
    5. Name the test: `test_collect_pending_updates_phase18_sc1_regression` or `test_collect_pending_updates_phase18_sc3_regression`.

    ---

    **Branch SC4 — App detection missing (collectors/windows/apps.py):**

    1. Read `collectors/windows/apps.py` in full before editing.
    2. Identify the exact failure from the Divergence notes:
       - If NinjaOne not detected: check that all 3 NinjaOne keywords (`NinjaRMMAgent`, `NinjaRMM`, `NinjaOne Agent`) are present in the keyword list passed to `_search_uninstall_keys()`.
       - If CrowdStrike not detected: check CrowdStrike keyword list (`CrowdStrike Windows Sensor`, `CrowdStrike Sensor Platform`) and service state read.
       - If M365 not collapsing to single suite entry: fix the M365 single-suite collapse logic.
       - If Company Portal not detected on enrolled machine: check the MSIX + MDM Enrollment registry path (`HKLM\SOFTWARE\Microsoft\Enrollments` or current path in code).
    3. ALWAYS use all 4 hive paths (HKLM, HKLM\Wow6432Node, HKCU, HKCU\Wow6432Node) in `_search_uninstall_keys()` calls — never enumerate fewer hives per CLAUDE.md constraint.
    4. In `tests/test_app_collector.py`, add a regression test for each failing detection. Reuse existing helpers (`_make_query_fn`, `_make_enum_fn`) — do not duplicate patterns.
    5. Name the test: `test_<appname>_detection_phase18_sc4_regression` (e.g. `test_ninjarmm_detection_phase18_sc4_regression`).

    ---

    **Branch SC5 — HTML render not correct (renderer/__init__.py):**

    1. Read `renderer/__init__.py` in full before editing.
    2. Identify the exact failure from the Divergence notes:
       - Dark CSS not applied: check CSS loading via `importlib.resources` — do NOT switch to filesystem path.
       - Layout broken: check Jinja2 template rendering and context passing.
       - Missing section: check that the AuditReport fields for the failing section are passed correctly to the template.
    3. Apply the minimum targeted fix. CSS MUST be loaded via `importlib.resources` — filesystem path loading is incompatible with PyInstaller --onedir packaging.
    4. In `tests/test_renderer.py`, add a regression test that renders a minimal AuditReport and asserts the specific structure that was missing is now present in the HTML output.
    5. Name the test: `test_render_html_phase18_sc5_regression`.

    ---

    **After all fix branches are applied:**

    - Run: `python -m pytest -x -q` — ALL tests must pass before Task 3.
    - If tests fail after the fix, correct the implementation (not the test) — the test is the specification.
    - Do NOT remove any existing test or weaken any existing assertion. Regressions are not acceptable.

    Constraints (apply to all branches):
    - Every code change in this task has a matching regression test added in the same task (D-09 pairing discipline).
    - The CollectionResult(value, error) envelope is NEVER violated — no raises across layer boundaries.
    - ALWAYS enumerate all 4 registry hives for any app detection change (CLAUDE.md constraint).
    - CSS must be loaded via importlib.resources — no filesystem paths in renderer (PyInstaller --onedir constraint).
    - Do NOT write any output to C:\, %TEMP%, %APPDATA%, or any host PC path (CLAUDE.md constraint).
    - SCRY output path derives from Path(sys.executable).parent — do not use os.getcwd() (CLAUDE.md constraint).
  </action>
  <verify>
    <automated>python -m pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - For each failing SC named in Task 1's resume signal, the corresponding production code fix is present (grep the exact symptom keyword in the modified file shows the fix)
    - For each code change, a regression test with `phase18_sc` in the name is present in the corresponding test file
    - `python -m pytest -x -q` passes with all tests green — no pre-existing tests removed or weakened
    - If Task 1 was `option-no-changes`, this task was skipped: `git diff collectors/ health_checks.py renderer/ tests/` shows no changes since Plan 18-01
  </acceptance_criteria>
  <done>
    - Every failing SC from Task 1 has a targeted code fix and a paired regression test
    - Full test suite passes
    - If Task 1 was `option-no-changes`, this task was skipped and no code files were modified
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomically close Phase 18 — update STATE.md, REQUIREMENTS.md, ROADMAP.md, run full suite</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (full file — modifying frontmatter, Current Position, Blockers/Concerns, Session Continuity)
    - .planning/REQUIREMENTS.md (full file — ticking VALID-01/VALID-03/VALID-05 checkboxes and traceability table rows)
    - .planning/ROADMAP.md (full file — ticking Phase 18 checkbox and updating plan count)
    - .planning/phases/18-live-machine-validation-system-health-and-apps/18-VALIDATION-RESULTS.md (confirm all SCs have a final disposition before closing)
  </read_first>
  <action>
    Step A — Run the full test suite as a gate BEFORE any planning-doc edits. Do NOT proceed until this passes:
    ```
    python -m pytest -x -q
    ```
    If anything fails, STOP and report. Task 2 (if it ran) is the only source of new code in this plan; any failure points back to Task 2's changes.

    Step B — Update `.planning/STATE.md`. Apply these deterministic edits exactly:

    **B.1 — Frontmatter:**
    - `status:` change from `context-gathered` to `phase-complete`
    - `stopped_at:` change to `Phase 18 complete — 3/3 plans, VALID-01/VALID-03/VALID-05 closed (see 18-VALIDATION-RESULTS.md)`
    - `last_updated:` set to today's date in ISO format (e.g. `"2026-05-21T00:00:00Z"`)
    - `last_activity:` change to `<today> — Phase 18 (Live Machine Validation — System Health and Apps) executed; SC1–SC5 validated; VALID-01/VALID-03/VALID-05 closed`
    - `progress.completed_phases:` increment from 2 to 3
    - `progress.completed_plans:` increment from 5 to 8 (Phase 18 adds 3 plans: 18-01, 18-02, 18-03)
    - `progress.total_plans:` increment from 5 to 8 (same 3 plans now count)
    - `progress.percent:` recompute as `(completed_phases / total_phases) * 100` = 3/4 = 75 (write integer `75`)

    **B.2 — Current Position section:**
    - `Phase:` change to `19 of 19 (Live Machine Validation — Vendor and Mac)`
    - `Plan:` reset to `—`
    - `Status:` set to `Ready to discuss`
    - `Last activity:` line same as frontmatter `last_activity`
    - `Progress:` bar update: `[███████░░░] 75%`

    **B.3 — Blockers/Concerns section:**
    - The current Blockers/Concerns section (from Phase 17's close) reads:
      ```
      - **Phase 19 gate:** Confirmed registry paths (Phase 17) may require code updates that must ship before live Dell/Lenovo validation in Phase 19
      - **Phase 18/19 gate:** Requires access to real enrolled Windows machines (SYSTEM/Admin account, Dell hardware, Intune-enrolled machine) and a real Mac
      ```
    - REMOVE the `Phase 18/19 gate` line entirely — Phase 18's real-hardware validation is now complete.
    - KEEP the `Phase 19 gate` line — it is still relevant (Phase 17's confirmed registry paths must ship before Phase 19 validates vendor rows on a real Dell).
    - Result: Blockers/Concerns section should contain exactly 1 line after this edit.

    **B.4 — Session Continuity section (if present):**
    - `Last session:` set to today's ISO timestamp
    - `Stopped at:` mirror the frontmatter `stopped_at`
    - `Resume file:` change to `.planning/ROADMAP.md`
    - `Next action:` change to `Run /gsd-discuss-phase 19` (or `/gsd-plan-phase 19`)

    Step C — Update `.planning/REQUIREMENTS.md`. Apply these deterministic edits:

    **C.1 — Top-of-file requirement checkboxes (three ticks):**
    - `- [ ] **VALID-01**:` → `- [x] **VALID-01**:`
    - `- [ ] **VALID-03**:` → `- [x] **VALID-03**:`
    - `- [ ] **VALID-05**:` → `- [x] **VALID-05**:`
    - VALID-02 and VALID-04 remain unticked (Phase 19 scope).

    **C.2 — Traceability table — add/update rows for VALID-01/VALID-03/VALID-05:**
    The traceability table currently has these VALID rows:
    ```
    | VALID-01 | Phase 18 | —                    | pending  |
    | VALID-03 | Phase 18 | —                    | pending  |
    | VALID-05 | Phase 18 | —                    | pending  |
    ```
    Rewrite these three rows as:
    ```
    | VALID-01 | Phase 18 | 18-01, 18-02, 18-03 | complete |
    | VALID-03 | Phase 18 | 18-01, 18-02, 18-03 | complete |
    | VALID-05 | Phase 18 | 18-01, 18-02, 18-03 | complete |
    ```
    Do NOT modify any other rows (VALID-02, VALID-04, DEBT-XX, CONF-XX rows are unchanged).

    Step D — Update `.planning/ROADMAP.md`. Apply these deterministic edits:

    **D.1 — Phase 18 progress table row:**
    Current row:
    ```
    | 18. Live Machine Validation — System Health and Apps | v3.1 | 0/TBD | Not started | - |
    ```
    Rewrite as:
    ```
    | 18. Live Machine Validation — System Health and Apps | v3.1 | 3/3 | Complete | <today's date> |
    ```

    **D.2 — Phase 18 section checkbox and plan count:**
    Current:
    ```
    - [ ] **Phase 18: Live Machine Validation — System Health and Apps** — IT staff validates uptime badges, pending updates, app detection, and HTML render on real enrolled Windows machines
    ```
    Rewrite as:
    ```
    - [x] **Phase 18: Live Machine Validation — System Health and Apps** — 3/3 plans — completed <today's date>
    ```

    **D.3 — Phase 18 Plans section (under `### Phase 18:`):**
    Update `**Plans:** TBD` to `**Plans:** 3 plans` and add the plan list:
    ```
    Plans:
    - [x] 18-01-PLAN.md — Create 18-VALIDATION-RESULTS.md skeleton with SC2 pre-populated (VALID-01, VALID-03, VALID-05)
    - [x] 18-02-PLAN.md — Edgar/IT validates SC1/SC3/SC4/SC5 on enrolled Windows machines (checkpoint:human-action)
    - [x] 18-03-PLAN.md — Conditional code fixes (if any SC failed) and phase close (VALID-01, VALID-03, VALID-05)
    ```

    Step E — Run the full test suite one final time to confirm STATE/REQUIREMENTS/ROADMAP edits didn't break any test:
    ```
    python -m pytest -x -q
    ```

    Constraints:
    - Do NOT delete `18-VALIDATION-RESULTS.md` — it is the permanent evidence artifact.
    - Do NOT modify Phase 19 planning in ROADMAP.md — that phase has its own discuss/plan cycle.
    - Do NOT remove the Phase 19 gate blocker line from STATE.md — it is still relevant.
    - If Task 2 applied code fixes, do NOT revisit or revert them in this task — they were already tested.
  </action>
  <verify>
    <automated>python -m pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Full test suite passes (no regressions)
    - `grep "status: phase-complete" .planning/STATE.md` returns one match
    - `grep "completed_phases: 3" .planning/STATE.md` returns one match
    - `grep "percent: 75" .planning/STATE.md` returns one match
    - `grep "Phase 19 of 19" .planning/STATE.md` returns one match (Current Position section)
    - `grep "Phase 18/19 gate" .planning/STATE.md` returns NO matches (line removed in B.3)
    - `grep "- \[x\] \*\*VALID-01" .planning/REQUIREMENTS.md` returns one match
    - `grep "- \[x\] \*\*VALID-03" .planning/REQUIREMENTS.md` returns one match
    - `grep "- \[x\] \*\*VALID-05" .planning/REQUIREMENTS.md` returns one match
    - `grep -E "VALID-01.*18-01, 18-02, 18-03.*complete" .planning/REQUIREMENTS.md` returns one match
    - `grep -E "VALID-03.*18-01, 18-02, 18-03.*complete" .planning/REQUIREMENTS.md` returns one match
    - `grep -E "VALID-05.*18-01, 18-02, 18-03.*complete" .planning/REQUIREMENTS.md` returns one match
    - `grep "\[x\].*Phase 18" .planning/ROADMAP.md` returns at least one match (Phase 18 checkbox ticked)
    - `grep "3/3" .planning/ROADMAP.md` returns at least one match in the Phase 18 section
    - `18-VALIDATION-RESULTS.md` still exists in the phase directory (not deleted)
  </acceptance_criteria>
  <done>
    - Full test suite passes
    - STATE.md shows `status: phase-complete`, `completed_phases: 3`, `percent: 75`, Current Position pointing to Phase 19
    - STATE.md `Phase 18/19 gate` blocker line removed; `Phase 19 gate` line retained
    - REQUIREMENTS.md: VALID-01/VALID-03/VALID-05 checkboxes ticked; traceability rows show `18-01, 18-02, 18-03 | complete`
    - ROADMAP.md: Phase 18 checkbox ticked, plan count shows 3/3, completion date added
    - `18-VALIDATION-RESULTS.md` remains in phase directory as permanent evidence artifact
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `18-VALIDATION-RESULTS.md` -> Plan 18-03 code edits | Any divergent values copied into production code originate from Edgar's observations in a markdown file. Trust assumption: Edgar is a trusted operator with legitimate access to fleet machines. |
| Conditional fix -> production .exe | Code changes in Task 2 ship in every future SCRY .exe run on Master Electronics hardware. A wrong fix would cause silent detection misses or incorrect display. |
| Plan 18-03 execution -> planning docs | STATE.md/REQUIREMENTS.md/ROADMAP.md edits are deterministic based on the validation evidence. No human judgment needed beyond Task 1 confirmation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | T (Tampering) | Divergence notes pasted into production collector code | mitigate | Task 1 is a `checkpoint:decision` — the user explicitly confirms the symptom before Task 2 applies a fix. Every code change is paired with a regression test that uses the exact same observed behavior; a wrong fix would fail the test on the next CI run. |
| T-18-09 | S (Spoofing) | Fraudulent FAIL disposition triggering unnecessary code changes | mitigate | Task 1 re-reads `18-VALIDATION-RESULTS.md` directly (not just the resume signal) and confirms with the user. The git history of `18-VALIDATION-RESULTS.md` shows when entries were added, providing an audit trail for any disputed disposition. |
| T-18-10 | E (Elevation of Privilege) | Plan 18-03 execution | accept | No new privilege escalation paths are introduced. The conditional fix branches are limited to keyword list updates, privilege-check logic, and rendering fixes — all within SCRY's existing, well-bounded scope. |
| T-18-11 | I (Information Disclosure) | SCRY output path (sys.executable.parent) | accept | SCRY already writes output to the flash drive only — no host PC writes. Task 3 makes no changes to this behavior. The constraint is enforced by CLAUDE.md and existing code; Plan 18-03 does not change it. |
| T-18-12 | D (Denial of Service) | PyInstaller --onedir packaging constraint | accept | Any Task 2 fix that touches renderer/ must use importlib.resources for CSS loading — filesystem path loading would break the --onedir .exe. This constraint is enforced by the action text and CLAUDE.md. No new DoS surface introduced. |

Plan 18-03 introduces no new attack surface in the no-changes path. In the fix path, changes are additive (keyword list entries) or corrective (privilege check, CSS loading) within the existing collector and renderer boundaries.
</threat_model>

<verification>
- Full test suite passes after Task 2 (if it ran) and after Task 3 (final check)
- `18-VALIDATION-RESULTS.md` Summary table shows all five SCs with a final disposition (no `_pending_` rows)
- For every FAIL disposition that triggered Task 2: a targeted code fix AND a matching regression test exist (grep `phase18_sc` in the relevant test file)
- STATE.md frontmatter shows `status: phase-complete`, `completed_phases: 3`, `percent: 75`
- STATE.md `Phase 18/19 gate` blocker line is removed; `Phase 19 gate` line is present
- REQUIREMENTS.md: VALID-01/VALID-03/VALID-05 checkboxes ticked; traceability rows show `complete` with Phase 18 plan references
- ROADMAP.md: Phase 18 checkbox ticked; plan count 3/3; completion date present
- `18-VALIDATION-RESULTS.md` remains in the phase directory (not deleted)
</verification>

<success_criteria>
- Phase 18 ROADMAP Success Criteria SC1–SC5: satisfied by `18-VALIDATION-RESULTS.md` evidence entries (plus Justin's SC2 pre-validation)
- VALID-01 closes: SC1 (Admin/SYSTEM uptime + pending updates) and SC2 (badge states) and SC3 (non-admin "N/A") all evidenced
- VALID-03 closes: SC4 (NinjaOne, CrowdStrike, M365 sign-off, Company Portal) evidenced by Edgar's run
- VALID-05 closes: SC5 (HTML character sheet in real browser) evidenced by Edgar's run
- Phase 19 unblocked: STATE.md pointer advanced; Phase 19 is the only remaining validation phase
- REQUIREMENTS.md traceability table is the canonical source of "which plan closed which requirement" — Phase 18 follows the deterministic 3-column format established in Phase 17
</success_criteria>

<output>
After completion, create `.planning/phases/18-live-machine-validation-system-health-and-apps/18-03-SUMMARY.md` using the standard summary template. The summary MUST record:
  - Which option from Task 1 was chosen (no-changes, or which SCs failed with symptoms)
  - The exact code changes applied (if any) and the matching regression test names added
  - The final test count delta (e.g. "+0 tests" or "+2 tests")
  - Confirmation that STATE.md, REQUIREMENTS.md, and ROADMAP.md were updated with the correct deterministic values
  - Confirmation that `18-VALIDATION-RESULTS.md` remains in the phase directory as permanent evidence
</output>
