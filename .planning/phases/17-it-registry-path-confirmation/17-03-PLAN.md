---
phase: 17-it-registry-path-confirmation
plan: 03
type: execute
wave: 3
depends_on: [17-01, 17-02]
files_modified:
  - collectors/windows/vendor.py
  - tests/test_vendor_collector.py
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CONF-01, CONF-02]
requirements_addressed: [CONF-01, CONF-02]
user_setup: []

must_haves:
  truths:
    - "If `17-IT-CONFIRMATION.md` reports any CONFIRMED-DIVERGENT disposition for Dell, the new DisplayName variant is appended to the DCU keyword list in `collectors/windows/vendor.py` AND a parameterized unit test in `tests/test_vendor_collector.py` exercises the new value with `report.dell_dcu.installed is True` (D-13)"
    - "If `17-IT-CONFIRMATION.md` reports any CONFIRMED-DIVERGENT disposition for Lenovo, the new DisplayName variant is appended to the LSU keyword list in `vendor.py` AND a parameterized unit test exercises the new value with `report.lenovo_lsu.installed is True` (D-13)"
    - "If `17-IT-CONFIRMATION.md` reports a DCU XML path different from the current `DCU_XML_PATH` constant, the constant in `vendor.py` is updated to the new path AND a constant-value regression test asserts `vendor_mod.DCU_XML_PATH == '<new path>'` (D-14)"
    - "If both CONF-IDs are CONFIRMED-MATCH (no divergence), `vendor.py` is unchanged in this plan and a one-line entry in `17-IT-CONFIRMATION.md` records 'no code changes required; closing on confirmed-current basis'"
    - "If either CONF-ID is NEGATIVE-RESULT, `vendor.py` is unchanged for that vendor and the negative result is documented in `17-IT-CONFIRMATION.md` with inspected hostnames (D-15, D-16); the CONF-ID closes anyway"
    - "`.planning/STATE.md` no longer contains the line that mentions Dell Command Update and Lenovo System Update registry paths as unconfirmed blockers"
    - "`.planning/REQUIREMENTS.md` traceability table shows CONF-01 and CONF-02 with their Phase 17 plan reference and a non-`pending` status"
  artifacts:
    - path: "collectors/windows/vendor.py"
      provides: "Updated keyword lists / DCU_XML_PATH constant if divergence found; otherwise unchanged"
    - path: "tests/test_vendor_collector.py"
      provides: "New parameterized tests for any discovered DisplayName variant or XML path; otherwise unchanged"
    - path: ".planning/STATE.md"
      provides: "Blockers/Concerns section no longer contains the registry-path uncertainty line; status advanced to phase-complete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONF-01 and CONF-02 marked complete in traceability table"
  key_links:
    - from: "Plan 17-03 execution"
      to: "17-IT-CONFIRMATION.md"
      via: "Read the Summary table dispositions before any code change"
      pattern: "CONFIRMED-MATCH|CONFIRMED-DIVERGENT|NEGATIVE-RESULT"
    - from: "Updated keyword list / DCU_XML_PATH"
      to: "Parameterized regression test"
      via: "Every divergent finding gets both a code change AND a test (D-13, D-14)"
      pattern: "test_dcu_keyword_variant_|test_lsu_keyword_variant_|test_dcu_xml_path_constant"
---

<objective>
Apply conditional patches based on Edgar's findings in `17-IT-CONFIRMATION.md` and atomically close Phase 17.

Purpose: Plan 17-01 shipped the diagnostic tool. Plan 17-02 captured Edgar's findings. Plan 17-03 is the decision step — read the Summary table in `17-IT-CONFIRMATION.md` and either (a) patch `vendor.py` per D-13/D-14 if any disposition is CONFIRMED-DIVERGENT, or (b) leave the code unchanged if both are CONFIRMED-MATCH or NEGATIVE-RESULT. Then close the phase: remove the registry-path blocker line from STATE.md, mark CONF-01 and CONF-02 complete in REQUIREMENTS.md, and write the closing SUMMARY.

This plan is `autonomous: false` because the branching depends on Edgar's findings (which Plan 17-02's resume signal already revealed). The executor reads the resume signal + the file and applies the conditional logic deterministically — no further human decision needed.

Output: A defensible code state matching what Edgar observed; clean STATE.md; closed CONF-01 and CONF-02 in REQUIREMENTS.md traceability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-it-registry-path-confirmation/17-CONTEXT.md
@.planning/phases/17-it-registry-path-confirmation/17-PATTERNS.md
@.planning/phases/17-it-registry-path-confirmation/17-IT-CONFIRMATION.md
@.planning/phases/17-it-registry-path-confirmation/17-01-SUMMARY.md
@.planning/phases/17-it-registry-path-confirmation/17-02-SUMMARY.md
@CLAUDE.md
@collectors/windows/vendor.py
@tests/test_vendor_collector.py

<interfaces>
<!-- Current production constants (before any patch). -->
<!-- Plan 17-03 changes ONE OR MORE of these only if Edgar's findings are CONFIRMED-DIVERGENT. -->

From collectors/windows/vendor.py:
```python
DCU_XML_PATH = r"C:\ProgramData\Dell\UpdateService\Temp\DCUApplicableUpdates.xml"

# DCU keyword list (line ~33):
["Dell Command Update", "Dell Command | Update"]

# LSU keyword list (lines ~67-71):
["Lenovo System Update", "Lenovo Vantage Service", "Lenovo Vantage", "Lenovo Commercial Vantage"]
```

Test helpers (already in tests/test_vendor_collector.py — reuse, do not modify):
- `_make_fake_ctx()`
- `_make_enum_fn(subkeys: list[str])`
- `_make_query_fn(display_name, display_version=None)`
- `make_report()`

Parameterized test analog (current lines 78-96):
```python
def test_dcu_installed_xml_present_two_updates(self, tmp_path):
    # ... uses patch.object(apps_mod.winreg, "QueryValueEx",
    #                       side_effect=_make_query_fn("Dell Command | Update", "5.5.0"))
    assert report.dell_dcu.installed is True
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Read `17-IT-CONFIRMATION.md` Summary table and confirm disposition before any code change</name>
  <decision>Which branch of the conditional patch logic applies (D-13, D-14, D-15, D-16)?</decision>
  <context>
    Plan 17-02's resume signal already conveyed the dispositions, but Plan 17-03 re-reads `17-IT-CONFIRMATION.md` directly to avoid drift. Before touching any code, confirm the disposition for each CONF-ID and the specific divergent values (if any). The executor reads the Summary table at the top of `17-IT-CONFIRMATION.md` and reports the decision back to the user for confirmation. This is a checkpoint:decision because the wrong branch produces silently wrong code — better to confirm explicitly.
  </context>
  <options>
    <option id="option-no-changes">
      <name>No code changes (both CONF-01 and CONF-02 are CONFIRMED-MATCH, or any combination of CONFIRMED-MATCH and NEGATIVE-RESULT)</name>
      <pros>Edgar confirmed current code is correct (or there's no fleet machine to disprove it). Plan 17-03 skips Task 2 entirely and goes straight to Task 3 (phase close).</pros>
      <cons>None — this is the simplest path.</cons>
    </option>
    <option id="option-dcu-keyword">
      <name>DCU keyword variant divergent (CONFIRMED-DIVERGENT on CONF-01 because Edgar saw a new Dell DisplayName)</name>
      <pros>Append the exact new string to the DCU keyword list per D-13. Add a parameterized test that mocks the registry with the new DisplayName and asserts `report.dell_dcu.installed is True`.</pros>
      <cons>None inherent — but the exact string must be copy-pasted verbatim from the `<details>` block in 17-IT-CONFIRMATION.md to avoid typos.</cons>
    </option>
    <option id="option-lsu-keyword">
      <name>LSU keyword variant divergent (CONFIRMED-DIVERGENT on CONF-02 because Edgar saw a new Lenovo DisplayName not in the 4-entry list)</name>
      <pros>Append the exact new string to the LSU keyword list per D-13. Add a parameterized test asserting `report.lenovo_lsu.installed is True`. Also add a new heading in the Phase 17 LSU comment block (added in Plan 17-01) labeled "Phase 17 Task 2 discoveries" naming the new entry — does NOT modify the existing D-09 Edgar-confirmed bullets, which are LOCKED at the 2-entry bound.</pros>
      <cons>None inherent.</cons>
    </option>
    <option id="option-dcu-xml-path">
      <name>DCU XML path divergent (CONFIRMED-DIVERGENT because the XML lives at a path other than the current `DCU_XML_PATH`)</name>
      <pros>Update the `DCU_XML_PATH` constant in `vendor.py` to the exact path Edgar observed per D-14. Add a constant-value regression test: `assert vendor_mod.DCU_XML_PATH == r"<new path>"`.</pros>
      <cons>Updating the constant changes behavior on every machine running --updates. The new path must be authoritative — confirmed on a real DCU-installed machine, not inferred. D-14 says "single source of truth — no fallback path list".</cons>
    </option>
    <option id="option-multiple">
      <name>Multiple divergences (e.g. DCU keyword variant AND XML path different, OR both vendors divergent)</name>
      <pros>Apply each branch's logic in sequence — append every new keyword, update the constant if needed, add every parameterized test.</pros>
      <cons>Each change must be paired with its own test (D-13/D-14 are not optional). Verify final test count includes one new test per divergence.</cons>
    </option>
  </options>
  <how-to-verify>
    1. Read `.planning/phases/17-it-registry-path-confirmation/17-IT-CONFIRMATION.md`. Find the Summary table.
    2. For CONF-01 row, read the Disposition column. For CONF-02 row, read the Disposition column.
    3. If either is CONFIRMED-DIVERGENT, scroll to the per-machine sections and identify:
       - For Dell divergence: the exact new DisplayName string from `**Matched DisplayName(s):**` that is NOT in `["Dell Command Update", "Dell Command | Update"]`, OR the exact new XML path string from `**DCU XML path observed:**` that is NOT `r"C:\ProgramData\Dell\UpdateService\Temp\DCUApplicableUpdates.xml"`.
       - For Lenovo divergence: the exact new DisplayName string from `**Matched DisplayName(s):**` that is NOT in `["Lenovo System Update", "Lenovo Vantage Service", "Lenovo Vantage", "Lenovo Commercial Vantage"]`.
    4. State the chosen option and the exact divergent values (if any) back to the user for confirmation.
  </how-to-verify>
  <resume-signal>
    Type one of:
      - `option-no-changes` (skip Task 2; proceed to Task 3 phase close)
      - `option-dcu-keyword: <exact new DisplayName>` (then Task 2 appends that string and adds a test)
      - `option-lsu-keyword: <exact new DisplayName>`
      - `option-dcu-xml-path: <exact new path string>` (raw string with backslashes preserved)
      - `option-multiple: <comma-separated list of divergent values, each tagged>` (e.g. `option-multiple: dcu-keyword="Dell Update Manager", xml-path="C:\OtherPath\DCUApplicableUpdates.xml"`)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: (Conditional) Apply patches per Task 1 decision + add parameterized regression tests</name>
  <files>collectors/windows/vendor.py, tests/test_vendor_collector.py</files>
  <read_first>
    - .planning/phases/17-it-registry-path-confirmation/17-IT-CONFIRMATION.md (the divergent values must be copy-pasted verbatim from here)
    - collectors/windows/vendor.py (full file — modifying keyword list and/or DCU_XML_PATH only)
    - tests/test_vendor_collector.py (full file — extending with parameterized tests; reuse `_make_fake_ctx`, `_make_enum_fn`, `_make_query_fn`)
    - .planning/phases/17-it-registry-path-confirmation/17-CONTEXT.md decisions D-09 (LOCKED Edgar-confirmed scope), D-13, D-14
    - .planning/phases/17-it-registry-path-confirmation/17-PATTERNS.md sections "Apply for D-13 keyword variants" and "Apply for D-14 new XML path"
    - CLAUDE.md "Critical Technical Constraints"
  </read_first>
  <action>
    SKIP THIS TASK ENTIRELY if Task 1's resume signal was `option-no-changes`. Verify with the user that Task 1 reported all CONFIRMED-MATCH and/or NEGATIVE-RESULT, then move directly to Task 3.

    Otherwise, apply each divergence branch named in Task 1's resume signal:

    ---

    **Branch A — DCU keyword variant (option-dcu-keyword OR option-multiple containing dcu-keyword):**

    1. In `collectors/windows/vendor.py`, locate the DCU keyword list in `_detect_dcu` (currently line 32-34):
       ```python
       installed, _version = _search_uninstall_keys(
           ["Dell Command Update", "Dell Command | Update"]
       )
       ```
       Append the new DisplayName from Task 1's resume signal AS THE LAST element of the list. Use the exact string verbatim — preserve casing, spacing, and special characters. Example if the new value is `"Dell Update Manager"`:
       ```python
       installed, _version = _search_uninstall_keys(
           ["Dell Command Update", "Dell Command | Update", "Dell Update Manager"]
       )
       ```

    2. In `tests/test_vendor_collector.py`, add a new test inside the `TestCollectVendorUpdates` class:
       ```python
       def test_dcu_installed_phase17_keyword_<sanitized>(self):
           """Phase 17 D-13: Edgar-confirmed Dell DisplayName variant from 17-IT-CONFIRMATION.md."""
           fake_ctx = _make_fake_ctx()
           with patch.object(apps_mod.winreg, "OpenKey", return_value=fake_ctx), \
                patch.object(apps_mod.winreg, "EnumKey",
                             side_effect=_make_enum_fn(["new variant key"])), \
                patch.object(apps_mod.winreg, "QueryValueEx",
                             side_effect=_make_query_fn("<EXACT NEW DISPLAYNAME>", "<VERSION FROM CONFIRMATION FILE>")), \
                patch.object(vendor_mod, "DCU_XML_PATH", "/nonexistent/path/x.xml"):
               report = make_report()
               vendor_mod.collect_vendor_updates(report)
           assert report.dell_dcu.installed is True
       ```
       Replace `<sanitized>` with a snake_case version of the DisplayName (e.g. "dell update manager" -> `dell_update_manager`). Replace `<EXACT NEW DISPLAYNAME>` and `<VERSION FROM CONFIRMATION FILE>` with the verbatim values from the per-machine entry.

    ---

    **Branch B — LSU keyword variant (option-lsu-keyword OR option-multiple containing lsu-keyword):**

    NOTE — D-09 IS LOCKED. Read `.planning/phases/17-it-registry-path-confirmation/17-CONTEXT.md §D-09` first.
    The 2 original "Edgar-confirmed entries" bullets in the Phase 17 LSU comment block (added in 17-01 Task 1) are LOCKED at `"Lenovo Vantage"` and `"Lenovo Commercial Vantage"`. Branch B MUST NOT modify those bullets, MUST NOT add a 5th bullet beneath them, and MUST NOT extend the D-09 Edgar-confirmed bound. The new entry goes under a NEW heading instead.

    1. In `collectors/windows/vendor.py`, locate the LSU keyword list in `_detect_lsu` (currently lines 66-71 plus the Phase 17 comment block added in Plan 17-01):
       ```python
       installed, _version = _search_uninstall_keys([
           "Lenovo System Update",
           "Lenovo Vantage Service",
           "Lenovo Vantage",
           "Lenovo Commercial Vantage",
       ])
       ```
       Append the new DisplayName from Task 1's resume signal AS THE LAST element. Then update the comment block above the list — but DO NOT touch the existing `# Edgar-confirmed entries (Phase 17 D-09, ...)` block or its two bullets (`"Lenovo Vantage"`, `"Lenovo Commercial Vantage"`). Instead, add a NEW heading directly above the keyword list and below the existing comment block:
       ```python
       # Phase 17 Task 2 discoveries (post-17-02 IT-CONFIRMATION.md update):
       #   - "<EXACT NEW DISPLAYNAME>"  (observed on <hostname>, see 17-IT-CONFIRMATION.md
       #     section "Machine: <HOSTNAME>")
       ```
       Rationale: D-09 bounds the Edgar-confirmed list to exactly 2 entries. Discoveries found via the Phase 17 `--diag-vendor` run are a separate post-discussion source of evidence, recorded under their own heading. The D-09 LOCKED state preserves the original discussion's exact scope.

    2. In `tests/test_vendor_collector.py`, add a parallel test:
       ```python
       def test_lsu_installed_phase17_keyword_<sanitized>(self):
           """Phase 17 D-13: Edgar-confirmed Lenovo DisplayName variant from 17-IT-CONFIRMATION.md."""
           fake_ctx = _make_fake_ctx()
           with patch.object(apps_mod.winreg, "OpenKey", return_value=fake_ctx), \
                patch.object(apps_mod.winreg, "EnumKey",
                             side_effect=_make_enum_fn(["new variant key"])), \
                patch.object(apps_mod.winreg, "QueryValueEx",
                             side_effect=_make_query_fn("<EXACT NEW DISPLAYNAME>", "<VERSION FROM CONFIRMATION FILE>")):
               report = make_report()
               vendor_mod.collect_vendor_updates(report)
           assert report.lenovo_lsu.installed is True
           assert report.lenovo_lsu.pending_count is None
           assert report.lenovo_lsu.scan_data_present is False
       ```

    ---

    **Branch C — DCU XML path (option-dcu-xml-path OR option-multiple containing xml-path):**

    1. In `collectors/windows/vendor.py`, update the `DCU_XML_PATH` constant on line 16:
       ```python
       # Before:
       DCU_XML_PATH = r"C:\ProgramData\Dell\UpdateService\Temp\DCUApplicableUpdates.xml"
       # After:
       DCU_XML_PATH = r"<EXACT NEW PATH FROM 17-IT-CONFIRMATION.md>"
       ```
       Use a raw string literal (`r"..."`) so backslashes are preserved verbatim. Single source of truth — do NOT create a fallback path list (D-14).

       Also update the comment on line 15 from `# Fixed path per D-11.` to `# Fixed path per Phase 17 D-14 (Edgar-confirmed in 17-IT-CONFIRMATION.md; supersedes Phase 14 D-11).`.

    2. In `tests/test_vendor_collector.py`, add a constant-value regression test outside any class (at module level, end of file):
       ```python
       def test_dcu_xml_path_constant_matches_phase17_confirmation():
           """Phase 17 D-14: DCU_XML_PATH constant reflects Edgar-confirmed path from 17-IT-CONFIRMATION.md."""
           assert vendor_mod.DCU_XML_PATH == r"<EXACT NEW PATH>"
       ```

    ---

    Constraints (apply to all branches):
    - Use raw string literals for any path value (preserve backslashes).
    - Copy values VERBATIM from `17-IT-CONFIRMATION.md` — do not transform, normalize, or simplify.
    - Do NOT remove any existing keyword from either list. Divergent findings ADD entries; they do not replace them.
    - Do NOT remove or rename `_detect_dcu`, `_detect_lsu`, `collect_vendor_updates`, or `diag_vendor_paths`.
    - Do NOT modify the D-09 Edgar-confirmed bullets in the LSU comment block — they are LOCKED at exactly 2 entries (`"Lenovo Vantage"`, `"Lenovo Commercial Vantage"`). New discoveries go under a separate `# Phase 17 Task 2 discoveries` heading.
    - Every code change in this task has a matching test added in the same task. D-13/D-14 are non-negotiable about pairing.
    - If multiple branches apply, run each branch's steps in sequence (A then B then C if all three triggered).
  </action>
  <verify>
    <automated>python -m pytest tests/test_vendor_collector.py -x -v</automated>
  </verify>
  <done>
    - For each divergent finding named in Task 1's resume signal, the corresponding code change is present (verifiable via Grep for the exact new value in `collectors/windows/vendor.py`)
    - For each code change, a matching parameterized or constant-value test is present in `tests/test_vendor_collector.py`
    - All tests in `tests/test_vendor_collector.py` pass — both pre-existing and newly added
    - The D-09 Edgar-confirmed bullets (`"Lenovo Vantage"`, `"Lenovo Commercial Vantage"`) are byte-for-byte unchanged in the LSU comment block (any LSU discovery added under a separate "Phase 17 Task 2 discoveries" heading instead)
    - If Task 1's resume signal was `option-no-changes`, this task was skipped and `git diff collectors/windows/vendor.py tests/test_vendor_collector.py` shows no changes since Plan 17-01 committed
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomically close Phase 17 — update STATE.md, REQUIREMENTS.md, and run full suite</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md (full file — modifying Current Position, progress, Blockers/Concerns, Session Continuity)
    - .planning/REQUIREMENTS.md (full file — updating Traceability table for CONF-01 and CONF-02, drive-by updating DEBT-01/02/03 rows)
    - .planning/phases/17-it-registry-path-confirmation/17-PATTERNS.md section ".planning/STATE.md (modify — clear blocker lines, advance pointer)"
    - .planning/phases/17-it-registry-path-confirmation/17-IT-CONFIRMATION.md (confirm both CONF-IDs have a final disposition)
    - .planning/phases/16-tech-debt-cleanup/16-01-PLAN.md frontmatter `requirements:` field (confirms DEBT-01 → Plan 16-01)
    - .planning/phases/16-tech-debt-cleanup/16-02-PLAN.md frontmatter `requirements:` field (confirms DEBT-02, DEBT-03 → Plan 16-02)
  </read_first>
  <action>
    Step A — Run the full test suite first as a gate. Do NOT proceed with STATE.md/REQUIREMENTS.md edits until this passes:
    ```
    python -m pytest -x -q
    ```
    If anything fails, STOP and report — phase 17 cannot close with regressions. Plan 17-03 task 2 (if it ran) is the only source of new code in this plan, so any failure points back to task 2's changes.

    Step B — Update `.planning/STATE.md`. Apply the following deterministic edits exactly:

    **B.1 — Blockers/Concerns section — 3-line deterministic rewrite:**

    Current STATE.md lines 57-59 read:
    ```
    - **Phase 18 gate:** Dell Command Update and Lenovo System Update registry paths unconfirmed — requires scheduling meeting with Edgar/IT before Phase 18 can complete
    - **Phase 20 gate:** Depends on Phase 18 completing first (confirmed paths may require code updates before live Dell/Lenovo validation)
    - **Phase 19/20 gate:** Requires access to real enrolled Windows machines (SYSTEM/Admin account, Dell hardware, Intune-enrolled machine) and a real Mac
    ```

    Note on phase numbering: Phase 17 IS this phase (IT Registry Path Confirmation). The current ROADMAP has Phase 18 = Live Machine Validation — System Health and Apps; Phase 19 = Live Machine Validation — Vendor and Mac. There is no Phase 20 (commit `a9a9db0` removed the old Phase 17 "Requirements Automation Hook"). The "Phase 20 gate" line is stale.

    Apply EXACTLY these three line-level edits (do not reorder, do not paraphrase):

    - **Line 57 — DELETE entirely.** The DCU/LSU paths are now confirmed by 17-IT-CONFIRMATION.md; this is the blocker that Phase 17 removes.
    - **Line 58 — REWRITE** from "Phase 20 gate: Depends on Phase 18 completing first (confirmed paths may require code updates before live Dell/Lenovo validation)" to **"Phase 19 gate: Confirmed registry paths (Phase 17) may require code updates that must ship before live Dell/Lenovo validation in Phase 19"**. (Phase 19 = Live Machine Validation — Vendor and Mac; THIS phase 17 is where the paths get confirmed; if any patches were applied in Task 2 they must reach Phase 19.)
    - **Line 59 — REWRITE** "Phase 19/20 gate" to "Phase 18/19 gate" (the substantive content — real-hardware access — is unchanged; only the phase numbers are corrected since there is no Phase 20).

    Result after edit (the Blockers/Concerns section should contain exactly 2 lines):
    ```
    - **Phase 19 gate:** Confirmed registry paths (Phase 17) may require code updates that must ship before live Dell/Lenovo validation in Phase 19
    - **Phase 18/19 gate:** Requires access to real enrolled Windows machines (SYSTEM/Admin account, Dell hardware, Intune-enrolled machine) and a real Mac
    ```

    **B.2 — Frontmatter updates:**
    - `status:` change from `context-gathered` to `phase-complete`
    - `stopped_at:` change to `Phase 17 complete — 3/3 plans, CONF-01 and CONF-02 closed (see 17-IT-CONFIRMATION.md)`
    - `last_updated:` set to today's date in ISO format
    - `last_activity:` change to `<today> — Phase 17 (IT Registry Path Confirmation) executed; --diag-vendor shipped; 17-IT-CONFIRMATION.md populated; <result summary>`
    - `progress.completed_phases:` increment by 1 (was 1 → becomes 2)
    - `progress.completed_plans:` increment by 3 (was 2 → becomes 5)
    - `progress.total_plans:` increment by 3 (was 2 → becomes 5; Phase 17 adds 3 plans to the milestone total)
    - `progress.percent:` compute as `(completed_phases / total_phases) * 100` → 2/4 = 50% (write integer `50`).
      - Note: the existing baseline reads `percent: 20` for `completed_phases: 1, total_phases: 4` (correct value would have been 25%). DO NOT preserve the drift. Write the new correct value (50%) computed from the formula. Mention the pre-existing baseline drift in the commit message (e.g. "fix: also correct pre-existing STATE.md percent drift baseline 20→25→50") but do NOT flag it as a phase 17 issue or open a separate ticket.

    **B.3 — Current Position section:**
    - `Phase:` change to `18 of 19 (Live Machine Validation — System Health and Apps)` (or whatever the next not-started phase is per ROADMAP)
    - `Plan:` reset to `—`
    - `Status:` set to `Ready to discuss` (or `Ready to plan` if discuss is being skipped)
    - `Last activity:` line same as the frontmatter `last_activity`

    **B.4 — Session Continuity section (only if present in current STATE.md):**
    - `Last session:` set to today's ISO timestamp
    - `Stopped at:` mirror the frontmatter `stopped_at`
    - `Resume file:` change to `.planning/ROADMAP.md` (next phase needs roadmap, not phase 17 context)
    - `Next action:` change to `Run /gsd-discuss-phase 18` (or `/gsd-plan-phase 18`)

    Step C — Update `.planning/REQUIREMENTS.md`. Apply the following deterministic table edits:

    **C.1 — Traceability table (bottom of file) — lock the deterministic format:**

    The existing rows MUST be rewritten as follows in a single edit pass:

    | REQ-ID  | Phase    | Plan                  | Status   |
    |---------|----------|-----------------------|----------|
    | DEBT-01 | Phase 16 | 16-01                 | complete |
    | DEBT-02 | Phase 16 | 16-02                 | complete |
    | DEBT-03 | Phase 16 | 16-02                 | complete |
    | CONF-01 | Phase 17 | 17-01, 17-02, 17-03   | complete |
    | CONF-02 | Phase 17 | 17-01, 17-02, 17-03   | complete |

    Notes on the DEBT rows (drive-by per W7 — no row had been "closed" in the traceability table yet before this edit, despite PROJECT.md post-Phase 16 treating them as validated):
      - DEBT-01 → Plan 16-01 (confirmed: `.planning/phases/16-tech-debt-cleanup/16-01-PLAN.md` frontmatter `requirements: [DEBT-01]`)
      - DEBT-02 → Plan 16-02 (confirmed: `16-02-PLAN.md` frontmatter `requirements: [DEBT-02, DEBT-03]`)
      - DEBT-03 → Plan 16-02 (same)
      The VALID-01/03/05 rows for Phase 18 and VALID-02/04 for Phase 19 are NOT touched — those rows close in their own phases.

    **C.2 — Top-of-file requirement checkboxes:**
    - `- [ ] **CONF-01**:` → `- [x] **CONF-01**:`
    - `- [ ] **CONF-02**:` → `- [x] **CONF-02**:`
    - `- [ ] **DEBT-01**:` → `- [x] **DEBT-01**:` (drive-by; Phase 16 closed but checkbox never ticked)
    - `- [ ] **DEBT-02**:` → `- [x] **DEBT-02**:` (drive-by)
    - `- [ ] **DEBT-03**:` → `- [x] **DEBT-03**:` (drive-by)
    - VALID-01/02/03/04/05 checkboxes remain unticked.

    Step D — Commit verification: run `python -m pytest -x -q` one more time to confirm STATE.md/REQUIREMENTS.md edits didn't break any test that reads those files (the GSD framework occasionally has frontmatter-validating tests).

    Constraints:
    - Do NOT touch ROADMAP.md in this task — phase close is a STATE+REQUIREMENTS edit. ROADMAP marking-as-shipped happens at milestone close, not phase close.
    - Do NOT advance to Phase 18 in this task — only update the pointer to "ready to discuss/plan 18". Phase 18 needs its own discuss step.
    - Do NOT delete `17-IT-CONFIRMATION.md` — it's the evidence artifact and must stay in the phase directory permanently.
    - Do NOT modify the real-hardware gate (now relabeled "Phase 18/19 gate" per B.1 line 59 rewrite). That dependency is genuine — Phase 17 only addresses the registry-path subset of the original blocker.
    - Do NOT preserve the pre-existing `percent: 20` drift in STATE.md frontmatter — recompute deterministically from the formula (B.2).
  </action>
  <verify>
    <automated>python -m pytest -x -q</automated>
  </verify>
  <done>
    - Full test suite passes
    - `grep "Dell Command Update and Lenovo System Update registry paths unconfirmed" .planning/STATE.md` returns no matches (B.1 line 57 deleted)
    - `grep "Phase 20" .planning/STATE.md` returns no matches (B.1 line 58 rewritten; no Phase 20 anywhere in STATE.md)
    - `grep "Phase 19 gate" .planning/STATE.md` returns at least one match (B.1 line 58 rewrite)
    - `grep "Phase 18/19 gate" .planning/STATE.md` returns at least one match (B.1 line 59 rewrite)
    - STATE.md frontmatter shows `status: phase-complete` and `percent: 50` (B.2; drift corrected)
    - `grep -E "CONF-01.*17-01, 17-02, 17-03.*complete" .planning/REQUIREMENTS.md` returns one match
    - `grep -E "CONF-02.*17-01, 17-02, 17-03.*complete" .planning/REQUIREMENTS.md` returns one match
    - `grep -E "DEBT-01.*16-01.*complete" .planning/REQUIREMENTS.md` returns one match
    - `grep -E "DEBT-02.*16-02.*complete" .planning/REQUIREMENTS.md` returns one match
    - `grep -E "DEBT-03.*16-02.*complete" .planning/REQUIREMENTS.md` returns one match
    - `grep "- \[x\] \*\*CONF-01" .planning/REQUIREMENTS.md` and `grep "- \[x\] \*\*CONF-02" .planning/REQUIREMENTS.md` both return matches
    - `grep "- \[x\] \*\*DEBT-01" .planning/REQUIREMENTS.md`, `DEBT-02`, `DEBT-03` all return matches (drive-by ticks)
    - STATE.md Current Position section names the next phase (18, not 17)
    - `17-IT-CONFIRMATION.md` is still present in the phase directory (not deleted)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `17-IT-CONFIRMATION.md` -> Plan 17-03 code edits | The divergent values copied into `vendor.py` originate from Edgar's run pasted into a markdown file. Trust assumption: Edgar is a trusted operator with legitimate access to fleet machines. |
| Plan 17-03 -> production code | Updated keyword list / DCU_XML_PATH constant ships in every future SCRY .exe. A wrong value would cause silent vendor-detection misses or false positives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | T (Tampering) | DisplayName values pasted into `vendor.py` keyword list | mitigate | Task 1 is a `checkpoint:decision` — the user explicitly confirms the divergent value before Task 2 applies it. Every code change is paired with a parameterized test (D-13/D-14) that uses the exact same value, so a wrong/tampered value would fail the test on the next CI run. |
| T-17-09 | S (Spoofing) | DCU_XML_PATH constant change | mitigate | Updating the constant requires a CONFIRMED-DIVERGENT disposition AND a `**DCU XML path observed:**` line in the per-machine evidence section showing `exists=true`. A claim of a new path without `exists=true` is rejected (the path Edgar ran against must exist). |
| T-17-10 | E (Elevation of Privilege) | Plan 17-03 execution | accept | No new code paths shipped beyond the keyword list / constant updates. The new tests run under pytest with no elevated privileges. |

Plan 17-03 introduces no new attack surface. The code changes (if any) are additive — appending strings to keyword lists or updating a single constant. Both Plan 17-01's threat model and the production collector's existing threat surface apply unchanged.
</threat_model>

<verification>
- Full test suite passes after every step (Task 2 if it ran, Task 3 final check) — no regressions to any previously passing test
- `17-IT-CONFIRMATION.md` Summary table shows both CONF-IDs with a final disposition (CONFIRMED-MATCH, CONFIRMED-DIVERGENT, or NEGATIVE-RESULT)
- For every CONFIRMED-DIVERGENT disposition, both a code change AND a matching test exist (verifiable via Grep)
- STATE.md Blockers/Concerns no longer mentions DCU/LSU registry path uncertainty
- STATE.md contains no "Phase 20" reference (the stale phase number is fully purged)
- STATE.md frontmatter `percent` is `50` (recomputed from formula; baseline drift corrected)
- REQUIREMENTS.md traceability table shows CONF-01 and CONF-02 as complete with Phase 17 plan refs; DEBT-01/02/03 also closed with Phase 16 plan refs (drive-by)
- Top-of-file requirement checkboxes for CONF-01, CONF-02, DEBT-01, DEBT-02, DEBT-03 are ticked
</verification>

<success_criteria>
- Phase 17 ROADMAP Success Criteria #1 (Dell): satisfied by `17-IT-CONFIRMATION.md` Dell entry + any required vendor.py keyword/path update + matching test
- Phase 17 ROADMAP Success Criteria #2 (Lenovo): satisfied by `17-IT-CONFIRMATION.md` Lenovo entry + any required vendor.py keyword update + matching test
- Phase 17 ROADMAP Success Criteria #3 (no regressions + blocker removal): satisfied by Task 3 (full suite passes + STATE.md blocker line removed)
- CONF-01 and CONF-02 close atomically (per D-08)
- Phase 18 unblocked — STATE.md pointer advanced; real-hardware gate remains as Phase 18/19's concern only
- `vendor.py` code state is now defensible: either matches what Edgar observed, or has been updated to match, or current code is documented as defensible against negative-result evidence
- REQUIREMENTS.md traceability table is the canonical source of "which plan closed which requirement" — Phase 17 establishes the deterministic 3-column format for future phases to follow
</success_criteria>

<output>
After completion, create `.planning/phases/17-it-registry-path-confirmation/17-03-SUMMARY.md` using the standard summary template. The summary MUST record:
  - Which option from Task 1 was chosen (no-changes, dcu-keyword, lsu-keyword, dcu-xml-path, or multiple)
  - The exact divergent values applied (if any) and the matching test names added
  - The final test count delta (e.g. "+0 tests" or "+2 tests")
  - Confirmation that the STATE.md blocker line was removed and REQUIREMENTS.md CONF-01/CONF-02 are marked complete
  - Confirmation that the drive-by DEBT-01/02/03 traceability rows were closed in the same edit
  - Confirmation that the STATE.md `percent` drift baseline (20 → expected 25 → new 50) was corrected
</output>
