---
phase: 01-models-and-hostname-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .venv/  # created by this plan
  - requirements-dev.txt
autonomous: true
requirements:
  - COLL-01
  - OUT-03

must_haves:
  truths:
    - "pytest can be invoked via .venv and runs to completion"
    - "requirements-dev.txt records the dev dependency for reproducibility"
    - "The venv uses Python 3.12 (not a system Python fallback)"
  artifacts:
    - path: ".venv/"
      provides: "Isolated Python 3.12 environment with pytest installed"
    - path: "requirements-dev.txt"
      provides: "Dev dependency pin for pytest"
  key_links:
    - from: "Python 3.12.10"
      to: ".venv"
      via: "python -m venv .venv"
      pattern: "python.exe inside .venv/Scripts/"
    - from: ".venv"
      to: "pytest"
      via: "pip install pytest==8.*"
      pattern: "pytest importable in .venv"
---

<objective>
Create the isolated Python 3.12 virtual environment and install pytest so all subsequent plans have a working test runner.

Purpose: pytest is not installed in the system Python environment (verified in research). Every subsequent plan assumes a venv exists at `.venv/` with pytest available. This plan creates that prerequisite before any source code is written.

Output: `.venv/` directory at project root with Python 3.12 interpreter + pytest 8.x; `requirements-dev.txt` pinning the dev dependency.
</objective>

<execution_context>
@C:/Users/justin.rhoda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justin.rhoda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/c/Users/justin.rhoda/status.report/.planning/PROJECT.md
@/c/Users/justin.rhoda/status.report/.planning/ROADMAP.md
@/c/Users/justin.rhoda/status.report/.planning/STATE.md
@/c/Users/justin.rhoda/status.report/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create venv and install pytest</name>
  <files>
    .venv/ (directory created by python -m venv)
    requirements-dev.txt
  </files>
  <read_first>
    - CLAUDE.md — confirms Python 3.12, confirms project root is C:\Users\justin.rhoda\status.report\
    - .planning/RESEARCH.md — Environment Availability table confirming Python 3.12.10 path and pytest not installed
  </read_first>
  <action>
    Run from the project root (C:\Users\justin.rhoda\status.report\):

    Step 1 — Create the virtual environment using Python 3.12:
      C:\Users\justin.rhoda\AppData\Local\Programs\Python\Python312\python.exe -m venv .venv

    Step 2 — Install pytest 8.x into the venv:
      .venv\Scripts\pip install "pytest==8.*"

    Step 3 — Create requirements-dev.txt at project root with exactly:
      pytest==8.*

    Do NOT use `pip freeze` for requirements-dev.txt — write the version constraint directly.
    Do NOT activate the venv globally; always invoke via .venv\Scripts\python or .venv\Scripts\pytest.
  </action>
  <verify>
    .venv\Scripts\python.exe --version
    # Must output: Python 3.12.x

    .venv\Scripts\pytest --version
    # Must output: pytest 8.x.x

    type requirements-dev.txt
    # Must contain: pytest==8.*
  </verify>
  <acceptance_criteria>
    - `.venv\Scripts\python.exe --version` outputs a string starting with "Python 3.12"
    - `.venv\Scripts\pytest --version` outputs a string starting with "pytest 8."
    - `requirements-dev.txt` exists at project root and contains "pytest==8.*"
    - `.venv\Scripts\pip show pytest` exits 0 and shows Version starting with "8."
  </acceptance_criteria>
  <done>Virtual environment created at .venv/ with Python 3.12, pytest 8.x installed, requirements-dev.txt written</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip registry → local venv | Package downloaded from PyPI during install; only dev tool, never shipped in the .exe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | pip install (PyPI download) | accept | pytest is a dev-only dependency never bundled into the distribution; supply-chain risk is limited to the developer machine, not the audit tool itself |
| T-01-01-02 | Information Disclosure | .venv/ committed to git | mitigate | Add `.venv/` to .gitignore before committing; venv contains no secrets but wastes repo space and is machine-specific |
</threat_model>

<verification>
From project root, both commands must succeed:

  .venv\Scripts\python.exe -c "import pytest; print(pytest.__version__)"
  # Expected: 8.x.x

  .venv\Scripts\python.exe -c "import sys; print(sys.version)"
  # Expected: 3.12.x ...
</verification>

<success_criteria>
- .venv/ exists and uses Python 3.12
- pytest 8.x is importable inside the venv
- requirements-dev.txt exists with "pytest==8.*"
- No system-level packages were modified
</success_criteria>

<output>
After completion, create `.planning/phases/01-models-and-hostname-parser/01-01-SUMMARY.md` following the summary template.
</output>
