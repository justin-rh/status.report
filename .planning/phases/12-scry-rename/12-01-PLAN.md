---
phase: 12-scry-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - scry.spec
  - build.bat
  - models.py
  - writers/__init__.py
autonomous: true
requirements:
  - RENAME-01
  - RENAME-02

must_haves:
  truths:
    - "main.py emits 'SCRY -- Master Electronics IT Audit Tool' on every run"
    - "Output filename for PHX-INV-001 on 2026-05-15 is 2026-05-15_scry_PHX-INV-001.html"
    - "scry.spec builds scry_v3.0 directory; status_report.spec no longer exists"
    - "build.bat invokes pyinstaller scry.spec and references dist\\scry_v3.0\\"
    - "writers/__init__.py writes scry.html, not status_report.html"
  artifacts:
    - path: "scry.spec"
      provides: "PyInstaller build spec for SCRY"
      contains: "name=f'scry_{VERSION}'"
    - path: "main.py"
      provides: "SCRY entry point with updated filename format"
      contains: "f\"{date_str}_scry_{hostname}\""
    - path: "build.bat"
      provides: "Build script referencing scry.spec"
      contains: "pyinstaller scry.spec"
    - path: "writers/__init__.py"
      provides: "HTML writer producing scry.html"
      contains: "scry.html"
  key_links:
    - from: "main.py"
      to: "output filename"
      via: "base_name = f\"{date_str}_scry_{hostname}\""
      pattern: "date_str.*scry.*hostname"
    - from: "scry.spec"
      to: "dist/scry_v3.0/"
      via: "name=f'scry_{VERSION}'"
      pattern: "name=f'scry_"
---

<objective>
Rename all source-layer files from StatusReport to SCRY. This is a pure mechanical
find-and-replace pass over main.py, the build spec (renamed from status_report.spec to
scry.spec), build.bat, models.py, and writers/__init__.py.

Purpose: Establish the SCRY name at the source layer so all subsequent phases build
under the new identity. Phases 13–15 depend on this rename being complete.

Output: scry.spec replacing status_report.spec; main.py with updated banner, prog=,
description=, and output filename format; build.bat referencing scry.spec and scry_v3.0;
writers/__init__.py writing to scry.html.
</objective>

<execution_context>
@C:/Users/justin.rhoda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justin.rhoda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename spec file and update all string references in main.py, build.bat, models.py, writers/__init__.py</name>
  <files>main.py, build.bat, models.py, writers/__init__.py</files>
  <read_first>main.py, build.bat, models.py, writers/__init__.py, status_report.spec</read_first>
  <action>
Perform all string replacements in source files. Then rename the spec file.

**main.py — exact replacements (line numbers from current file):**

Line 1 docstring: Change
  `"""StatusReport entry point. Phase 5.`
to:
  `"""SCRY entry point. Phase 5.`

Line 3 (docstring output path comment): Change
  `Output path: Path(sys.executable).parent / "logs" / "status_{hostname}_{date}.html"`
to:
  `Output path: Path(sys.executable).parent / "logs" / "{date}_scry_{hostname}.html"`

Line 94 (argparse description): Change
  `description="StatusReport -- Master Electronics IT Audit Tool",`
to:
  `description="SCRY -- Master Electronics IT Audit Tool",`

Line 95 (argparse prog): Change
  `prog="status_report",`
to:
  `prog="scry",`

Line 108 (print banner): Change
  `print("StatusReport -- Master Electronics IT Audit Tool")`
to:
  `print("SCRY -- Master Electronics IT Audit Tool")`

Lines 140–141 (output filename): Change
  `base_name = f"status_{hostname}_{date_str}"`
  `output_path = logs_dir / f"{base_name}.html"`
to:
  `base_name = f"{date_str}_scry_{hostname}"`
  `output_path = logs_dir / f"{base_name}.html"`

Note: The collision-handling loop on line 144 uses `base_name` as-is — it remains
unchanged and automatically uses the new format.

**models.py — exact replacement:**

Line 1: Change
  `"""StatusReport data contract. All layers import from this module.`
to:
  `"""SCRY data contract. All layers import from this module.`

**writers/__init__.py — exact replacements:**

Line 10 (docstring): Change
  `"""Write HTML string to output_path / 'status_report.html'. Returns full path.`
to:
  `"""Write HTML string to output_path / 'scry.html'. Returns full path.`

Line 16 (dest assignment): Change
  `dest = output_path / 'status_report.html'`
to:
  `dest = output_path / 'scry.html'`

**build.bat — exact replacements:**

Line 2: Change
  `REM StatusReport -- One-command build script`
to:
  `REM SCRY -- One-command build script`

Line 5: Change
  `REM After build: copy dist\status_report\ to the USB flash drive.`
to:
  `REM After build: copy dist\scry_v3.0\ to the USB flash drive.`

Line 19: Change
  `ECHO Building status_report with PyInstaller...`
to:
  `ECHO Building SCRY with PyInstaller...`

Line 20: Change
  `CALL pyinstaller status_report.spec --noconfirm`
to:
  `CALL pyinstaller scry.spec --noconfirm`

Line 28: Change
  `ECHO Build complete. Distributable is in: dist\status_report_v2.1\`
to:
  `ECHO Build complete. Distributable is in: dist\scry_v3.0\`

Line 29: Change
  `ECHO Copy dist\status_report_v2.1\ to the USB flash drive.`
to:
  `ECHO Copy dist\scry_v3.0\ to the USB flash drive.`

**Rename spec file (git mv — preserves history):**
```
git mv status_report.spec scry.spec
```

After the rename, make the following edits to scry.spec:

Line 1: Change
  `# StatusReport PyInstaller build spec -- D-08 (checked into repo).`
to:
  `# SCRY PyInstaller build spec -- D-08 (checked into repo).`

Line 2 (output comment): Change
  `# Output: dist/status_report_{VERSION}/ -- IT copies this folder to USB flash drive.`
to:
  `# Output: dist/scry_{VERSION}/ -- IT copies this folder to USB flash drive.`

Line 11 (VERSION): Change
  `VERSION = "v2.1"   # Update this when cutting a new milestone build.`
to:
  `VERSION = "v3.0"   # Update this when cutting a new milestone build.`

Line 78 (EXE name): Change
  `name=f'status_report_{VERSION}',`
to:
  `name=f'scry_{VERSION}',`

Line 98 (COLLECT name and comment): Change
  `name=f'status_report_{VERSION}',    # dist/status_report_{VERSION}/ is the distributable folder`
to:
  `name=f'scry_{VERSION}',    # dist/scry_{VERSION}/ is the distributable folder`
  </action>
  <verify>
    <automated>
grep -n "SCRY" main.py | head -5
grep -n "date_str.*scry.*hostname\|scry.*hostname\|{date_str}_scry" main.py
grep -n "status_report" main.py
grep "scry.html" writers/__init__.py
grep "status_report.html" writers/__init__.py
grep "SCRY" models.py
grep "scry.spec\|scry_v3.0" build.bat
grep "status_report" build.bat
ls scry.spec && echo "scry.spec exists"
ls status_report.spec 2>/dev/null && echo "WARNING: old spec still exists" || echo "old spec gone"
grep "VERSION.*v3.0\|name=f'scry_" scry.spec | head -5
    </automated>
  </verify>
  <acceptance_criteria>
- main.py line 1 docstring contains "SCRY entry point"
- main.py contains `f"{date_str}_scry_{hostname}"` (not `f"status_{hostname}_{date_str}"`)
- main.py contains `print("SCRY -- Master Electronics IT Audit Tool")`
- main.py contains `description="SCRY -- Master Electronics IT Audit Tool"`
- main.py contains `prog="scry"`
- main.py contains zero occurrences of "StatusReport"
- writers/__init__.py contains `dest = output_path / 'scry.html'`
- writers/__init__.py contains zero occurrences of "status_report"
- models.py line 1 docstring contains "SCRY data contract"
- scry.spec exists in repo root; status_report.spec does not exist
- scry.spec contains `VERSION = "v3.0"`
- scry.spec contains `name=f'scry_{VERSION}'` in both EXE and COLLECT sections
- scry.spec contains zero occurrences of "status_report"
- build.bat contains `CALL pyinstaller scry.spec --noconfirm`
- build.bat contains `dist\scry_v3.0\` in both ECHO lines
- build.bat contains zero occurrences of "status_report" or "StatusReport"
  </acceptance_criteria>
  <done>All five source files have been updated; status_report.spec is gone; scry.spec exists with VERSION=v3.0; output filename format is date-first; no "StatusReport" or "status_report" string remains in any of the five modified files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → source files | Mechanical rename; no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | scry.spec VERSION bump v2.1→v3.0 | accept | VERSION is a display string only; no security surface; build artifact name changes are cosmetic |
| T-12-02 | Information Disclosure | output filename date-first format | accept | Filename is on USB drive owned by IT staff; no PII exposed beyond hostname already in filename |
</threat_model>

<verification>
Run after both tasks complete:

```
grep -rn "StatusReport\|status_report" main.py models.py writers/ build.bat scry.spec
```

Expected: zero matches. Any match is a missed rename.

```
grep "date_str.*scry\|{date_str}_scry" main.py
```

Expected: one match showing the new filename format.

```
python -c "from writers import write_html; from pathlib import Path; import tempfile; d=tempfile.mkdtemp(); p=write_html('<html/>', Path(d)); print(p.name)"
```

Expected output: `scry.html`
</verification>

<success_criteria>
1. `grep -rn "StatusReport\|status_report" main.py models.py writers/ build.bat scry.spec` returns zero matches
2. `main.py` contains `f"{date_str}_scry_{hostname}"` for output filename
3. `scry.spec` exists; `status_report.spec` does not exist
4. `scry.spec` VERSION is `"v3.0"` and both `name=` lines use `f'scry_{VERSION}'`
5. `writers/__init__.py` writes to `scry.html`
</success_criteria>

<output>
After completion, create `.planning/phases/12-scry-rename/12-01-SUMMARY.md`
</output>
