---
phase: 12-scry-rename
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - CLAUDE.md
  - README.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - RENAME-01

must_haves:
  truths:
    - "CLAUDE.md project guide header and body reference SCRY, not StatusReport"
    - "README.md project name, CLI examples, output section, and build output reference SCRY"
    - ".planning/PROJECT.md project name and current state section reference SCRY"
    - ".planning/ROADMAP.md milestone header references SCRY (project title)"
    - ".planning/STATE.md references SCRY where it previously said StatusReport"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated project guide"
      contains: "# SCRY"
    - path: "README.md"
      provides: "Updated project readme"
      contains: "# SCRY"
    - path: ".planning/PROJECT.md"
      provides: "Updated project doc"
      contains: "# SCRY"
    - path: ".planning/ROADMAP.md"
      provides: "Updated roadmap title"
      contains: "# Roadmap: SCRY"
  key_links:
    - from: "CLAUDE.md"
      to: "developer mental model"
      via: "project name in header"
      pattern: "# SCRY"
    - from: "README.md"
      to: "IT staff instructions"
      via: "scry.exe and scry_v3.0 references"
      pattern: "scry.exe|scry_v3.0"
---

<objective>
Update all planning and project documentation to reference SCRY instead of StatusReport.
This is a documentation-layer rename parallel to the test rename in Plan 02.

Purpose: Docs must match the code. CLAUDE.md is the primary guide developers and Claude
read — it must lead with the new name. README.md is what IT staff reads for instructions.
PROJECT.md and the planning docs need to be consistent for Phase 13+ planning.

Output: CLAUDE.md, README.md, PROJECT.md, ROADMAP.md, STATE.md all updated; no
"StatusReport" in titles, headers, or user-facing instructions.
</objective>

<execution_context>
@C:/Users/justin.rhoda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justin.rhoda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md and README.md</name>
  <files>CLAUDE.md, README.md</files>
  <read_first>CLAUDE.md, README.md</read_first>
  <action>
**CLAUDE.md — exact replacements:**

Line 1 header: Change
  `# StatusReport — Project Guide`
to:
  `# SCRY — Project Guide`

Line 3 (What This Is body): Change
  `A self-contained Windows .exe (PyInstaller --onedir) that runs from a USB flash drive and audits a Windows PC.`
to:
  `A self-contained Windows .exe (PyInstaller --onedir) that runs from a USB flash drive and audits a Windows PC. (Project name: SCRY — formerly StatusReport.)`

Note: The rest of the CLAUDE.md body contains "StatusReport" only in the title — no
other occurrences need changing in this file. The technical constraints, stack, and
architecture sections do not contain the project name. Verify after the header change
that no other "StatusReport" remains.

**README.md — exact replacements:**

Line 1 header: Change
  `# StatusReport`
to:
  `# SCRY`

Line 31 (IT staff instructions, step 1): Change
  `1. Copy \`dist\status_report_v2.1\\` to a USB flash drive`
to:
  `1. Copy \`dist\scry_v3.0\\` to a USB flash drive`

Line 32 (step 2): Change
  `2. On the target PC, open the USB drive and double-click \`status_report.exe\``
to:
  `2. On the target PC, open the USB drive and double-click \`scry.exe\``

Line 34 (step 4 — output path): Change
  `5. The report is saved to \`logs\status_{hostname}_{date}.html\` on the USB drive`
to:
  `5. The report is saved to \`logs\{date}_scry_{hostname}.html\` on the USB drive`

CLI examples section (lines 53–58): Change all four command examples from
  `status_report.exe --name`
  `status_report.exe --serial`
  `status_report.exe --warnings`
  `status_report.exe --name --serial`
to:
  `scry.exe --name`
  `scry.exe --serial`
  `scry.exe --warnings`
  `scry.exe --name --serial`

Build section — Output line (line 87): Change
  `Output: \`dist\status_report_v2.1\\` — copy this folder to the USB flash drive.`
to:
  `Output: \`dist\scry_v3.0\\` — copy this folder to the USB flash drive.`

Project Structure section (line 154): Change
  `status_report.spec          # PyInstaller --onedir build definition`
to:
  `scry.spec                   # PyInstaller --onedir build definition`

Output section bottom (line 167): Change
  `The tool saves \`logs\status_{hostname}_{date}.html\` to the USB drive`
to:
  `The tool saves \`logs\{date}_scry_{hostname}.html\` to the USB drive`

After all replacements, verify no remaining "StatusReport" or "status_report" in README.md
that refer to the project name, executable, or output file. (References within the
Hostname Convention section and Detected Applications section do not mention
"StatusReport" — no changes needed there.)
  </action>
  <verify>
    <automated>
grep -n "StatusReport\|status_report" CLAUDE.md
grep -n "StatusReport\|status_report" README.md
    </automated>
  </verify>
  <acceptance_criteria>
- CLAUDE.md line 1 is `# SCRY — Project Guide`
- CLAUDE.md contains zero occurrences of "StatusReport" (grep returns empty)
- README.md line 1 is `# SCRY`
- README.md contains `scry.exe` in CLI examples
- README.md contains `dist\scry_v3.0\` in build instructions and IT staff instructions
- README.md contains `{date}_scry_{hostname}.html` in output description
- README.md contains `scry.spec` in project structure
- README.md contains zero occurrences of "StatusReport" or "status_report"
  </acceptance_criteria>
  <done>CLAUDE.md and README.md headers, instructions, and examples all reference SCRY; no StatusReport name remains in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/PROJECT.md, ROADMAP.md, and STATE.md</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>
**PROJECT.md — exact replacements:**

Line 1 header: Change
  `# StatusReport`
to:
  `# SCRY`

The body of PROJECT.md refers to "StatusReport" once in the header only. All other
project name references use the description form ("the tool", "this project") and do
not need updating. Verify after the header change that no remaining "StatusReport"
appears in user-visible sections.

**ROADMAP.md — exact replacements:**

Line 1 header: Change
  `# Roadmap: StatusReport`
to:
  `# Roadmap: SCRY`

The milestones table row for Phase 12 currently says:
  `| 12. SCRY Rename | v3.0 | 0/? | Not started | - |`

Update the Plans count from `0/?` to `3/3` and update Phase 12 entry in the phase
listing to add the plans. Specifically, find the Phase 12 detail block:

```
### Phase 12: SCRY Rename
**Goal**: ...
...
**Plans**: TBD
```

Change `**Plans**: TBD` to:
```
**Plans**: 3 plans
Plans:
- [ ] 12-01-PLAN.md — Source code renames (main.py, scry.spec, build.bat, models.py, writers/)
- [ ] 12-02-PLAN.md — Test file updates and full test suite verification (203 tests)
- [ ] 12-03-PLAN.md — Documentation updates (CLAUDE.md, README.md, planning docs)
```

Also update the progress table row for Phase 12 from `0/?` to `0/3` (not started yet,
plans now known):
  `| 12. SCRY Rename | v3.0 | 0/3 | Not started | - |`

**STATE.md — exact replacements:**

STATE.md prose references "StatusReport" in zero places — the state file already uses
"SCRY Rename" and "SCRY" consistently throughout (check before editing). If any
occurrence of "StatusReport" exists in the prose (not in the roadmap evolution history
which documents legacy phases), update it to "SCRY".

The roadmap evolution history line:
  `- v1.0 Phases 1–5: hostname parser, hardware collectors, D&D character sheet, app detection, PyInstaller packaging`
is historical and accurate — do not change historical references.

If STATE.md already contains no "StatusReport" in the prose body, no changes are needed.
Confirm with grep before and after.
  </action>
  <verify>
    <automated>
grep -n "StatusReport" .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md
grep "# SCRY" .planning/PROJECT.md .planning/ROADMAP.md
grep "# Roadmap: SCRY" .planning/ROADMAP.md
grep "12-01-PLAN\|12-02-PLAN\|12-03-PLAN" .planning/ROADMAP.md
    </automated>
  </verify>
  <acceptance_criteria>
- .planning/PROJECT.md line 1 is `# SCRY`
- .planning/ROADMAP.md line 1 is `# Roadmap: SCRY`
- .planning/ROADMAP.md Phase 12 block lists 3 plan files (12-01-PLAN.md, 12-02-PLAN.md, 12-03-PLAN.md)
- .planning/ROADMAP.md progress table shows `0/3` for Phase 12
- `grep "StatusReport" .planning/PROJECT.md` returns zero matches
- `grep "StatusReport" .planning/ROADMAP.md` returns zero matches
- STATE.md contains zero occurrences of "StatusReport" in prose (historical phase list lines are acceptable)
  </acceptance_criteria>
  <done>All three planning documents updated; ROADMAP.md reflects the three new plans; no "StatusReport" remains in doc headers or user-facing content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → developer/IT reader | Documentation only; no code execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | README.md output filename format | accept | Filename is on USB drive owned by IT staff; date-first format is IT-visible and intentional |
</threat_model>

<verification>
Final cross-check across all documentation:

```
grep -rn "StatusReport\|status_report" CLAUDE.md README.md .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md
```

Expected: zero matches for project name, executable name, or output filename references
using the old names. (Historical phase list entries in STATE.md that describe v1.0/v2.0
work may use descriptive terms — these are acceptable.)

```
grep -n "SCRY" CLAUDE.md README.md .planning/PROJECT.md .planning/ROADMAP.md
```

Expected: at least one match per file confirming the new name appears in each doc.
</verification>

<success_criteria>
1. `grep "StatusReport\|status_report" CLAUDE.md README.md .planning/PROJECT.md .planning/ROADMAP.md` returns zero matches
2. All five docs have "SCRY" in their header/title line
3. README.md instructions reference `scry.exe`, `scry_v3.0`, and the new filename format `{date}_scry_{hostname}.html`
4. ROADMAP.md Phase 12 entry lists all three plan files
</success_criteria>

<output>
After completion, create `.planning/phases/12-scry-rename/12-03-SUMMARY.md`
</output>
