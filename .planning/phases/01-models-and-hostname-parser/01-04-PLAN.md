---
phase: 01-models-and-hostname-parser
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - tests/__init__.py
  - tests/test_name_parser.py
  - collectors/__init__.py
  - collectors/base.py
  - collectors/windows/__init__.py
  - renderer/__init__.py
  - writers/__init__.py
autonomous: false
requirements:
  - COLL-01
  - OUT-03

must_haves:
  truths:
    - "pytest test suite runs to completion with zero failures and zero Windows API calls"
    - "All 21 city codes are exercised by at least one test case (not one test per code — representative sampling)"
    - "All four device types have at least one passing test"
    - "D-02, D-03, D-04, D-05, D-08, D-09 behaviors each have a dedicated test"
    - "OUT-03 is verified by test_no_exception_on_any_input()"
    - "ROADMAP SC4 satisfied: all 21 city codes and known dept codes covered"
    - "Stub directories exist for collectors/, renderer/, writers/"
  artifacts:
    - path: "tests/__init__.py"
      provides: "Package marker for tests/ directory"
    - path: "tests/test_name_parser.py"
      provides: "pytest unit tests for parse_hostname() — no Windows API calls"
      min_lines: 60
    - path: "collectors/__init__.py"
      provides: "Package stub for Phase 2"
    - path: "collectors/base.py"
      provides: "Placeholder for Phase 2 base collector class"
    - path: "collectors/windows/__init__.py"
      provides: "Package stub for Phase 2 Windows collectors"
    - path: "renderer/__init__.py"
      provides: "Package stub for Phase 3"
    - path: "writers/__init__.py"
      provides: "Package stub for Phase 5"
  key_links:
    - from: "tests/test_name_parser.py"
      to: "parsers.name_parser.parse_hostname"
      via: "from parsers.name_parser import parse_hostname"
      pattern: "import at top of test file"
    - from: "pytest"
      to: "tests/test_name_parser.py"
      via: ".venv\\Scripts\\pytest tests/"
      pattern: "zero failures, zero errors"
---

<objective>
Write the pytest test suite for the hostname parser, create stub package directories for future phases, and run a human verification checkpoint to confirm all tests pass.

Purpose: The tests establish the formal acceptance baseline for ROADMAP SC4 (all 21 city codes and known dept codes covered, no Windows API calls). The stub directories complete the project structure so Phase 2 has named packages to fill in. The checkpoint confirms the developer sees a clean green test run before the phase is marked complete.

Output: tests/__init__.py, tests/test_name_parser.py, collectors/ stub, renderer/ stub, writers/ stub. pytest exits 0 with all tests green.
</objective>

<execution_context>
@C:/Users/justin.rhoda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justin.rhoda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/c/Users/justin.rhoda/status.report/.planning/PROJECT.md
@/c/Users/justin.rhoda/status.report/.planning/ROADMAP.md
@/c/Users/justin.rhoda/status.report/.planning/STATE.md
@/c/Users/justin.rhoda/status.report/CLAUDE.md
@/c/Users/justin.rhoda/status.report/.planning/phases/01-models-and-hostname-parser/01-CONTEXT.md
@/c/Users/justin.rhoda/status.report/.planning/phases/01-models-and-hostname-parser/01-RESEARCH.md
@/c/Users/justin.rhoda/status.report/models.py
@/c/Users/justin.rhoda/status.report/parsers/name_parser.py

<interfaces>
<!-- Key exports from parsers/name_parser.py that tests import: -->

from parsers.name_parser import parse_hostname

parse_hostname(hostname: str) -> ParsedHostname

ParsedHostname fields:
  raw_hostname: str
  city: str | None
  device_type: str | None  — "Warehouse Workstation" | "User-Assigned Laptop" |
                              "Department Laptop" | "P3 Warehouse Device" | "Unknown"
  department: str | None
  company_code: str | None
  station: int | None

CITY_CODES keys (21 confirmed):
  AMM, AMS, CHI, EDE, GEO, GER, GUA, MIA, MON, NYC, PEO, PEN, PHX, ROC, SCL, SEA, SMO, TAM, TOR, TSU, VAN

<!-- Test design rules from CONTEXT.md: -->
D-07: Representative sample of known dept codes + one unknown-code test. NOT one test per known code.
D-07: Tests must not call any Windows API (no winreg, no wmi, no subprocess to system commands).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pytest test suite and stub directories</name>
  <files>
    tests/__init__.py
    tests/test_name_parser.py
    collectors/__init__.py
    collectors/base.py
    collectors/windows/__init__.py
    renderer/__init__.py
    writers/__init__.py
  </files>
  <read_first>
    - parsers/name_parser.py — read the actual implementation to understand what behaviors to test (executor must read this; it was created in Plan 03)
    - models.py — ParsedHostname field names for assertions
    - .planning/phases/01-models-and-hostname-parser/01-RESEARCH.md — "Pattern 3: Unit Test Structure" (lines 355-410), full parametrize example with all test cases
    - .planning/phases/01-models-and-hostname-parser/01-CONTEXT.md — D-07 (representative sample policy, not one test per code), D-01 through D-09 (each needs a test)
    - .planning/ROADMAP.md — Phase 1 SC4 ("all 21 city codes and all known department codes covered by unit tests that pass without any Windows API calls")
  </read_first>
  <action>
    Create tests/__init__.py:
      Content: # tests package — unit tests for StatusReport (no Windows API calls)

    Create tests/test_name_parser.py with the following test coverage. The parametrize
    table MUST cover ALL items below. Add additional cases as needed to satisfy SC4.

    REQUIRED TEST CASES (parametrize table, each as (hostname, {field: expected_value})):

    # ROADMAP success criteria (SC1, SC2, SC3) — must be named in comments
    ('PHX-INV-003',     {'city': 'Phoenix', 'device_type': 'Warehouse Workstation', 'department': 'INV', 'station': 3})
    ('PHX-ABC123-ME',   {'device_type': 'User-Assigned Laptop', 'company_code': 'ME'})
    ('DESKTOP-XYZ123',  {'device_type': 'Unknown', 'raw_hostname': 'DESKTOP-XYZ123', 'city': None})

    # P3 Warehouse Device
    ('CHI-P3B-002',     {'device_type': 'P3 Warehouse Device', 'city': 'Chicago', 'station': 2})
    ('PHX-P3A-001',     {'device_type': 'P3 Warehouse Device'})       # Pitfall 1 guard
    ('VAN-P3C-010',     {'device_type': 'P3 Warehouse Device', 'city': 'Vancouver'})

    # Department Laptop (LAP substring)
    ('NYC-INVLAP-004',  {'device_type': 'Department Laptop', 'department': 'INVLAP', 'city': 'New York'})
    ('MIA-SHPLAP-002',  {'device_type': 'Department Laptop', 'department': 'SHPLAP'})

    # D-02: unrecognized dept code still classifies as Warehouse Workstation
    ('PHX-NEWDEPT-005', {'device_type': 'Warehouse Workstation', 'department': 'NEWDEPT'})

    # D-03: unrecognized company code still classifies as User-Assigned Laptop
    ('PHX-ABC123-XX',   {'device_type': 'User-Assigned Laptop', 'company_code': 'XX'})

    # D-04: unrecognized city prefix
    ('ZZZ-INV-003',     {'device_type': 'Unknown', 'city': None})

    # D-08: fully unrecognized (no city prefix match, no structure)
    ('DESKTOP-XYZ123',  {'device_type': 'Unknown', 'city': None})

    # D-09: recognized city but too few segments
    ('PHX-BADSTRUCT',   {'city': 'Phoenix', 'device_type': 'Unknown'})

    # City code representative sample (D-07 — covering multiple geographic regions)
    # Must cover at least 8 of the 21 codes across different test cases total
    ('AMM-SHP-001',     {'city': 'Amman',        'device_type': 'Warehouse Workstation'})
    ('AMS-REC-003',     {'city': 'Amsterdam',    'device_type': 'Warehouse Workstation'})
    ('VAN-REC-007',     {'city': 'Vancouver',    'device_type': 'Warehouse Workstation'})
    ('TSU-P3A-003',     {'city': 'Tsuchiura',    'device_type': 'P3 Warehouse Device'})
    ('SEA-INV-012',     {'city': 'Seattle',      'device_type': 'Warehouse Workstation'})
    ('GUA-ABC-ME',      {'city': 'Guadalajara',  'device_type': 'User-Assigned Laptop'})
    ('TOR-ABC-ES',      {'city': 'Toronto',      'device_type': 'User-Assigned Laptop'})
    ('SCL-RECLAP-001',  {'city': 'Santiago',     'device_type': 'Department Laptop'})

    ADDITIONAL STANDALONE TESTS (not parametrize):

    def test_raw_hostname_always_preserved():
        """D-05: raw_hostname field preserved regardless of parse outcome."""
        for hostname in ['PHX-INV-003', 'DESKTOP-XYZ123', 'PHX-BADSTRUCT', 'ZZZ-X-X', 'phx-inv-003']:
            result = parse_hostname(hostname)
            assert result.raw_hostname == hostname, f'raw_hostname not preserved for {hostname!r}'

    def test_no_exception_on_any_input():
        """OUT-03: parser never raises on any string input."""
        bad_inputs = ['', '-', '--', 'PHX', 'phx-inv-003', '123-INV-003', 'PHX-INV',
                      'A' * 300, ';;;', '\x00\x01\x02']
        for bad in bad_inputs:
            result = parse_hostname(bad)   # must NOT raise
            assert result.device_type is not None, f'device_type is None for {bad!r}'

    def test_station_is_int_not_string():
        """ROADMAP SC1, Pitfall 2: station must be int, not '003'."""
        result = parse_hostname('PHX-INV-003')
        assert result.station == 3
        assert isinstance(result.station, int), f'station type={type(result.station)}'

    def test_lowercase_hostname_handled():
        """Pitfall 3: lowercase input produces same device_type as uppercase."""
        lower = parse_hostname('phx-inv-003')
        upper = parse_hostname('PHX-INV-003')
        assert lower.device_type == upper.device_type
        assert lower.city == upper.city
        assert lower.department == upper.department
        assert lower.station == upper.station
        assert lower.raw_hostname == 'phx-inv-003'   # D-05: original preserved

    def test_p3_not_misclassified_as_workstation():
        """Pitfall 1: PHX-P3A-001 must be P3 Warehouse Device, not Warehouse Workstation."""
        result = parse_hostname('PHX-P3A-001')
        assert result.device_type == 'P3 Warehouse Device', (
            f'Disambiguation order bug: P3A classified as {result.device_type!r}'
        )

    Test file imports:
      import pytest
      from parsers.name_parser import parse_hostname

    Do NOT import any of: winreg, wmi, subprocess, win32api, ctypes, or any Windows-specific module.

    Parametrize decorator format:
      @pytest.mark.parametrize('hostname,expected', [...])
      def test_parse_hostname(hostname, expected):
          result = parse_hostname(hostname)
          for field_name, value in expected.items():
              assert getattr(result, field_name) == value, (
                  f'{hostname}: expected {field_name}={value!r}, got {getattr(result, field_name)!r}'
              )

    Create stub directories (minimal content — just enough to establish the architecture):

    collectors/__init__.py:
      # collectors package — hardware and profile collectors (Phase 2)
      # Each collector returns CollectionResult(value, error) — never raises.

    collectors/base.py:
      # Base collector interface — Phase 2 implements concrete collectors here.
      # Platform-swappable: windows/ and mac/ subdirectories hold OS-specific implementations.

    collectors/windows/__init__.py:
      # Windows-specific collector implementations (Phase 2)

    renderer/__init__.py:
      # renderer package — Jinja2 HTML character sheet renderer (Phase 3)

    writers/__init__.py:
      # writers package — file output to flash drive (Phase 5)
  </action>
  <verify>
    .venv\Scripts\pytest tests/ -v
    # All tests must PASS; exit code 0; no errors; no warnings about missing imports

    .venv\Scripts\python.exe -c "import collectors; import renderer; import writers; print('stubs OK')"
    # Must print: stubs OK
  </verify>
  <acceptance_criteria>
    - `tests/__init__.py` exists
    - `tests/test_name_parser.py` exists with at least 60 lines
    - `grep -n "def test_" tests/test_name_parser.py` returns at least 5 matches (parametrize + 4 standalone)
    - `grep -n "winreg\|wmi\|subprocess\|win32" tests/test_name_parser.py` returns NO match (no Windows API calls)
    - `grep -n "DESKTOP-XYZ123" tests/test_name_parser.py` returns a match (SC3 / OUT-03 covered)
    - `grep -n "test_no_exception_on_any_input" tests/test_name_parser.py` returns a match
    - `grep -n "test_station_is_int" tests/test_name_parser.py` returns a match
    - `.venv\Scripts\pytest tests/ -v` exits 0 with all tests PASSED (no FAILED, no ERROR)
    - `collectors/__init__.py` exists
    - `collectors/base.py` exists
    - `collectors/windows/__init__.py` exists
    - `renderer/__init__.py` exists
    - `writers/__init__.py` exists
  </acceptance_criteria>
  <done>pytest suite written; all tests pass; stub directories created; no Windows API calls in test file</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Entire Phase 1 deliverable:
    - models.py with four dataclasses (CollectionResult, ParsedHostname, AppStatus, AuditReport)
    - parsers/__init__.py and parsers/name_parser.py (parse_hostname pure function, 21 city codes)
    - tests/__init__.py and tests/test_name_parser.py (full pytest suite, no Windows API calls)
    - Stub directories: collectors/, renderer/, writers/
  </what-built>
  <how-to-verify>
    Run each command from the project root (C:\Users\justin.rhoda\status.report\):

    1. Verify all five ROADMAP success criteria:

       .venv\Scripts\python.exe -c "from models import AuditReport, ParsedHostname, AppStatus, CollectionResult; print('SC5 PASS: all dataclasses importable')"

       .venv\Scripts\python.exe -c "
       from parsers.name_parser import parse_hostname
       r = parse_hostname('PHX-INV-003')
       assert r.city=='Phoenix' and r.device_type=='Warehouse Workstation' and r.department=='INV' and r.station==3
       print('SC1 PASS: PHX-INV-003 fully decoded')
       r2 = parse_hostname('PHX-ABC123-ME')
       assert r2.device_type=='User-Assigned Laptop' and r2.company_code=='ME'
       print('SC2 PASS: PHX-ABC123-ME decoded')
       r3 = parse_hostname('DESKTOP-XYZ123')
       assert r3.device_type=='Unknown' and r3.raw_hostname=='DESKTOP-XYZ123'
       print('SC3 PASS: DESKTOP-XYZ123 handled gracefully')
       "

    2. Run the full test suite:
       .venv\Scripts\pytest tests/ -v

       Expected output: all tests green, exit code 0.
       Note any test that shows FAILED or ERROR.

    3. Verify project structure is correct:
       dir /b (lists: CLAUDE.md, models.py, parsers\, tests\, collectors\, renderer\, writers\, requirements-dev.txt, .venv\)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all tests pass and all three SC verification commands print PASS.
    If any test fails, paste the pytest output — Claude will diagnose and fix.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test inputs → parse_hostname | Adversarial strings fed to the parser in test_no_exception_on_any_input(). Purpose: confirm parser doesn't eval or exec input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | test_no_exception_on_any_input — null bytes and metacharacters | mitigate | Test explicitly covers '\x00\x01\x02' and ';;;' to verify no shell expansion occurs. Parser uses str.split() only — null bytes pass through harmlessly to ParsedHostname.raw_hostname and are never passed to a shell or OS call. |
| T-01-04-02 | Information Disclosure | tests/ contain no secrets | accept | Test cases use synthetic hostnames (PHX-INV-003 etc.). No real production hostnames, credentials, or PII in the test file. |
| T-01-04-03 | Denial of Service | 'A' * 300 test input | mitigate | test_no_exception_on_any_input includes a 300-char string. Parser must handle it within normal Python string operation time. str.split('-') on a 300-char string is O(n) — no risk. |
</threat_model>

<verification>
Phase 1 complete when ALL of the following are true:

ROADMAP Phase 1 Success Criteria:
  SC1: parse_hostname('PHX-INV-003') -> city=Phoenix, device_type=Warehouse Workstation, department=INV, station=3 [PASS]
  SC2: parse_hostname('PHX-ABC123-ME') -> device_type=User-Assigned Laptop, company_code=ME [PASS]
  SC3: parse_hostname('DESKTOP-XYZ123') -> device_type=Unknown, raw_hostname=DESKTOP-XYZ123, no exception [PASS]
  SC4: pytest tests/ exits 0, all 21+ city code spot-checks pass, no Windows API calls [PASS]
  SC5: from models import AuditReport, ParsedHostname, AppStatus, CollectionResult succeeds [PASS]

Requirements addressed:
  COLL-01: Hostname decoded into city, device_type, department/company_code, station [PASS]
  OUT-03: Unrecognized hostname returns Unknown device_type with raw_hostname preserved, no exception [PASS]
</verification>

<success_criteria>
- pytest tests/ exits 0 with all tests green
- No test imports winreg, wmi, subprocess, or any Windows API module
- All five ROADMAP Phase 1 success criteria verified by the human checkpoint
- Stub directories (collectors/, renderer/, writers/) exist for Phase 2-5
- Phase 1 can be declared complete
</success_criteria>

<output>
After checkpoint approved, create `.planning/phases/01-models-and-hostname-parser/01-04-SUMMARY.md` following the summary template.
</output>
