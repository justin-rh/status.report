---
phase: 12-scry-rename
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - tests/test_main.py
  - tests/test_main_mac.py
  - tests/test_writers.py
  - tests/test_renderer.py
  - tests/__init__.py
autonomous: true
requirements:
  - RENAME-01
  - RENAME-02

must_haves:
  truths:
    - "All 203 tests pass after the rename"
    - "test_main.py patches sys.argv with 'scry' not 'status_report'"
    - "test_writers.py and test_renderer.py assert scry.html not status_report.html"
    - "tests/__init__.py comment references SCRY"
  artifacts:
    - path: "tests/test_main.py"
      provides: "Updated sys.argv patches using 'scry'"
      contains: "patch(\"sys.argv\", [\"scry\""
    - path: "tests/test_main_mac.py"
      provides: "Updated sys.argv patches using 'scry'"
      contains: "patch(\"sys.argv\", [\"scry\""
    - path: "tests/test_writers.py"
      provides: "Assertions against scry.html"
      contains: "scry.html"
    - path: "tests/test_renderer.py"
      provides: "Assertions against scry.html"
      contains: "scry.html"
  key_links:
    - from: "tests/test_main.py"
      to: "main.py prog= value"
      via: "patch(\"sys.argv\", [\"scry\", ...])"
      pattern: "sys.argv.*scry"
    - from: "tests/test_writers.py"
      to: "writers/__init__.py dest filename"
      via: "assert result.name == 'scry.html'"
      pattern: "scry.html"
---

<objective>
Update all test files to reference the new SCRY names. No test logic changes — only
string values that refer to "status_report" as the program name or "status_report.html"
as the output filename are updated. Then run the full 203-test suite to confirm zero
regressions.

Purpose: Tests that patch `sys.argv` with `["status_report", ...]` will mismatch the
new `prog="scry"` argparse declaration unless updated. Tests asserting
`status_report.html` will fail because writers/__init__.py now writes `scry.html`.

Output: Five test files with updated name strings; pytest exits 0 with 203 tests passed.
</objective>

<execution_context>
@C:/Users/justin.rhoda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justin.rhoda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-scry-rename/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update string references in all five test files</name>
  <files>tests/test_main.py, tests/test_main_mac.py, tests/test_writers.py, tests/test_renderer.py, tests/__init__.py</files>
  <read_first>tests/test_main.py, tests/test_main_mac.py, tests/test_writers.py, tests/test_renderer.py, tests/__init__.py</read_first>
  <action>
These are mechanical string replacements. Do NOT change any test logic, assertion
structure, helper function bodies, or comments explaining why tests exist.
Only change the literal string values that name the program or output file.

**tests/test_main.py — exact replacements:**

Every occurrence of:
  `patch("sys.argv", ["status_report"`
replace with:
  `patch("sys.argv", ["scry"`

There are 8 occurrences across the file (lines 65, 179, 193, 211, 230, 260, 281, 298,
315). Replace ALL of them. Verify after by checking that "status_report" no longer
appears as a sys.argv value anywhere in the file.

No changes to any assertion logic, mock structures, or comments.

**tests/test_main_mac.py — exact replacements:**

Every occurrence of:
  `patch("sys.argv", ["status_report"`
replace with:
  `patch("sys.argv", ["scry"`

There are 2 occurrences (lines 67 and 72 in the _patched_main_platform helper). Replace
both. No other changes.

**tests/test_writers.py — exact replacements:**

Replace ALL occurrences of the string `'status_report.html'` with `'scry.html'`.
This affects:
- Line 22: `assert (Path(tmp) / 'status_report.html').exists()`  →  `assert (Path(tmp) / 'scry.html').exists()`
- Line 32: `expected = Path(tmp) / 'status_report.html'`  →  `expected = Path(tmp) / 'scry.html'`
- Line 50: `assert result.name == 'status_report.html'`  →  `assert result.name == 'scry.html'`

Do NOT change any other lines. The test docstrings that say "creates status_report.html"
in the function description lines may be updated to say "scry.html" for accuracy, but
only the description string — not any assertion structure.

**tests/test_renderer.py — exact replacements:**

Replace ALL occurrences of the string `'status_report.html'` with `'scry.html'`.
This affects:
- Line 199: `assert out.name == 'status_report.html'`  →  `assert out.name == 'scry.html'`
- Line 269 (write_html test helper): `assert (Path(tmp) / 'status_report.html').exists()`  →  `assert (Path(tmp) / 'scry.html').exists()`

The test function docstrings that reference "status_report.html" should also be updated
to "scry.html" for accuracy.

**tests/__init__.py — exact replacement:**

Line 1: Change
  `# tests package — unit tests for StatusReport (no Windows API calls)`
to:
  `# tests package — unit tests for SCRY (no Windows API calls)`
  </action>
  <verify>
    <automated>
grep -n "status_report" tests/test_main.py tests/test_main_mac.py tests/test_writers.py tests/test_renderer.py tests/__init__.py
    </automated>
  </verify>
  <acceptance_criteria>
- `grep "status_report" tests/test_main.py` returns zero matches
- `grep "status_report" tests/test_main_mac.py` returns zero matches
- `grep "status_report.html" tests/test_writers.py` returns zero matches
- `grep "status_report.html" tests/test_renderer.py` returns zero matches
- tests/__init__.py line 1 contains "unit tests for SCRY"
- tests/test_main.py contains `patch("sys.argv", ["scry"` in every sys.argv patch
- tests/test_writers.py contains `'scry.html'` in all three assertion locations
- tests/test_renderer.py contains `'scry.html'` in all assertion locations
  </acceptance_criteria>
  <done>All five test files updated; no "status_report" string remains as a program name or filename value in any test file.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and confirm 203 tests pass</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the full pytest suite from the repo root to verify that all 203 tests pass with
zero failures and zero errors. This is the acceptance gate for the entire Phase 12 rename.

Command:
```
.venv/Scripts/pytest --tb=short -q
```

If any test fails:
1. Read the failure output carefully
2. Identify whether the failure is due to a missed rename (a "status_report" string
   that was not updated) or a test logic issue
3. For missed renames only: fix the specific string and re-run pytest
4. Do NOT change test assertions or test logic to make tests pass — fix the source
   file instead

Common failure patterns to look for:
- `AssertionError: 'status_report.html' != 'scry.html'` → missed rename in writers/__init__.py
- `AssertionError: expected scry.html but got status_report.html` → missed rename in writers
- Tests involving sys.argv failing → missed patch in test file
- `FileNotFoundError: status_report.spec` → spec was not renamed or build.bat not updated
  </action>
  <verify>
    <automated>.venv/Scripts/pytest --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- pytest exits with code 0
- Output contains "203 passed" (exact count) or a higher number if new tests were added
- Output contains "0 failed" and "0 error"
- No test was skipped due to rename-related issues
  </acceptance_criteria>
  <done>pytest exits 0; all 203 tests pass; the Phase 12 rename is verified correct at the code and test layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → source code | Tests verify source behavior; no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | test logic modification | mitigate | Task 1 action explicitly prohibits changing test logic; only literal string values change; pytest run in Task 2 confirms behavior is preserved |
</threat_model>

<verification>
After pytest passes:

```
grep -rn "status_report" tests/
```

Expected: zero matches for "status_report" as a program name or HTML filename. (The
string "StatusReport" may still appear in comments that explain historical context —
that is acceptable for test files as those comments are not assertions.)

```
.venv/Scripts/pytest --tb=short -q 2>&1 | grep -E "passed|failed|error"
```

Expected: `203 passed` with no failures.
</verification>

<success_criteria>
1. `grep -n "status_report" tests/test_main.py tests/test_main_mac.py` returns zero matches
2. `grep -n "status_report.html" tests/test_writers.py tests/test_renderer.py` returns zero matches
3. `pytest` exits 0 with 203 tests passed
</success_criteria>

<output>
After completion, create `.planning/phases/12-scry-rename/12-02-SUMMARY.md`
</output>
