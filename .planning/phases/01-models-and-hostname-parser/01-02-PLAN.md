---
phase: 01-models-and-hostname-parser
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - models.py
autonomous: true
requirements:
  - COLL-01
  - OUT-03

must_haves:
  truths:
    - "All four dataclasses are importable from models.py with a single import statement"
    - "CollectionResult uses Generic[T] and has an .ok property"
    - "ParsedHostname.raw_hostname is a required (non-optional) field; all others default to None"
    - "AuditReport.parsed_hostname field exists (not parsed_name)"
    - "AppStatus.installed is bool; AppStatus.service_state defaults to None"
    - "station field on ParsedHostname is typed int | None (not str)"
    - "No mutable class-level defaults — list fields use field(default_factory=list)"
  artifacts:
    - path: "models.py"
      provides: "Four dataclasses: CollectionResult, ParsedHostname, AppStatus, AuditReport"
      exports:
        - CollectionResult
        - ParsedHostname
        - AppStatus
        - AuditReport
      min_lines: 60
  key_links:
    - from: "models.py"
      to: "parsers/name_parser.py"
      via: "from models import ParsedHostname"
      pattern: "ParsedHostname imported by parser in Phase 1 Plan 03"
    - from: "models.py"
      to: "Phase 3 renderer"
      via: "AuditReport passed to Jinja2 template"
      pattern: "AuditReport.parsed_hostname.device_type used in template"
---

<objective>
Define all four dataclasses in models.py — the single source-of-truth data contract that every subsequent phase imports from.

Purpose: This is the hub of the architecture. ParsedHostname shape is locked by D-01 through D-09. AuditReport must be wide enough to hold Phase 2 hardware fields, Phase 3 rendering fields, and Phase 4 app detection results. Designing correctly now prevents breaking changes in later phases.

Output: models.py at project root containing CollectionResult[T], ParsedHostname, AppStatus, and AuditReport as @dataclass classes.
</objective>

<execution_context>
@C:/Users/justin.rhoda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justin.rhoda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/c/Users/justin.rhoda/status.report/.planning/PROJECT.md
@/c/Users/justin.rhoda/status.report/.planning/ROADMAP.md
@/c/Users/justin.rhoda/status.report/.planning/STATE.md
@/c/Users/justin.rhoda/status.report/CLAUDE.md
@/c/Users/justin.rhoda/status.report/.planning/phases/01-models-and-hostname-parser/01-CONTEXT.md
@/c/Users/justin.rhoda/status.report/.planning/phases/01-models-and-hostname-parser/01-RESEARCH.md

<interfaces>
<!-- Locked decisions governing ParsedHostname field design. Executor MUST implement exactly. -->

From CONTEXT.md decisions:
  D-01: Device type determined by segment structure (not code whitelist)
  D-02: Unrecognized dept code -> device_type=Warehouse Workstation, department preserved as raw value
  D-03: Unrecognized company code -> device_type=User-Assigned Laptop, company_code preserved
  D-04: Unrecognized city -> device_type=Unknown, all other fields None
  D-05: raw_hostname ALWAYS preserved regardless of outcome
  D-08: Fully unrecognized hostname -> device_type=Unknown, all other fields None
  D-09: Recognized city but <3 segments -> city populated, device_type=Unknown, rest None

From ROADMAP SC1 (authoritative field names):
  parse result: city=Phoenix, device_type=Warehouse Workstation, department=INV, station=3
  NOTE: field name is 'department' (NOT 'dept_code' — dept_code appears in CONTEXT.md D-02 but
        ROADMAP SC1 is the binding authority for field names)
  NOTE: field name is 'parsed_hostname' on AuditReport (NOT 'parsed_name' from ARCHITECTURE.md)
  NOTE: station is int (3, not '003')

AppStatus consumed by:
  Phase 3 renderer: needs installed (bool), version (str|None) for badge display
  Phase 4 detector: sets installed, version, service_state, detection_method, error

CollectionResult consumed by:
  Phase 2 collectors: every collector wraps its return value in CollectionResult
  Caller pattern: result = collector(); if result.ok: use(result.value) else: log(result.error)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write models.py with all four dataclasses</name>
  <files>models.py</files>
  <read_first>
    - .planning/phases/01-models-and-hostname-parser/01-RESEARCH.md — "Complete models.py" code example (lines 468-537), "Pattern 1: Dataclass Data Contract" (lines 195-257), Anti-Patterns section, Assumptions Log (A1: station=int, A2: parsed_hostname field name, A3: department field name)
    - .planning/phases/01-models-and-hostname-parser/01-CONTEXT.md — decisions D-01 through D-09, Claude's Discretion section (AppStatus field design)
    - CLAUDE.md — architecture diagram: main.py → collectors/ → models.AuditReport → renderer/ → writers/
  </read_first>
  <behavior>
    - from models import CollectionResult, ParsedHostname, AppStatus, AuditReport succeeds
    - CollectionResult(value=42).ok == True
    - CollectionResult(value=None, error='oops').ok == False
    - ParsedHostname(raw_hostname='PHX-INV-003').raw_hostname == 'PHX-INV-003'
    - ParsedHostname(raw_hostname='x').city is None  # defaults to None
    - AuditReport(hostname='h', parsed_hostname=ParsedHostname(raw_hostname='h')).apps == []  # default_factory
    - Two AuditReport instances do NOT share the same apps list object
    - AppStatus(name='NinjaOne', installed=True).service_state is None  # optional field defaults to None
  </behavior>
  <action>
    Create models.py at the project root (C:\Users\justin.rhoda\status.report\models.py).

    The file MUST contain exactly these four classes in this order, with these exact field names:

    1. CollectionResult(Generic[T]):
       - value: T | None
       - error: str | None = None
       - property ok -> bool: return self.error is None
       - TypeVar T defined above the class

    2. ParsedHostname:
       - raw_hostname: str                   (required, no default — D-05)
       - city: str | None = None             (full city name or None — D-04)
       - device_type: str | None = None      ("Warehouse Workstation" | "User-Assigned Laptop" |
                                              "Department Laptop" | "P3 Warehouse Device" | "Unknown")
       - department: str | None = None       (warehouse dept code — D-02, NOT 'dept_code')
       - company_code: str | None = None     (laptop company code — D-03)
       - station: int | None = None          (int, e.g. 3 not '003' — ROADMAP SC1)

    3. AppStatus:
       - name: str                           (human-readable app name, required)
       - installed: bool                     (primary flag for Phase 3 badge)
       - version: str | None = None          (registry DisplayVersion)
       - service_state: str | None = None    ('Running' | 'Stopped' | None)
       - detection_method: str = 'registry'  ('registry' | 'filesystem' | 'service')
       - error: str | None = None

    4. AuditReport:
       - hostname: str                       (required)
       - parsed_hostname: ParsedHostname     (required — NOT 'parsed_name', NOT optional)
       - os_version: str | None = None
       - os_build: str | None = None
       - cpu_model: str | None = None
       - ram_gb: float | None = None
       - disk_total_gb: float | None = None
       - disk_free_gb: float | None = None
       - current_user: str | None = None
       - local_profiles: list[str] = field(default_factory=list)
       - apps: list[AppStatus] = field(default_factory=list)
       - collection_errors: list[str] = field(default_factory=list)
       - timestamp: str = ''

    Imports at top of file:
      from dataclasses import dataclass, field
      from typing import Generic, TypeVar

    Use Python 3.12 union syntax throughout (str | None, NOT Optional[str]).
    Use lowercase generics for list fields (list[str], NOT List[str]).
    Do NOT use frozen=True on any dataclass — Phase 2 collectors populate fields after construction.
    Do NOT import from parsers/ — models.py has zero upward dependencies.

    Add a module-level docstring identifying this as the data contract:
      """StatusReport data contract. All layers import from this module.
      ROADMAP SC5: AuditReport, ParsedHostname, AppStatus, CollectionResult importable here.
      """
  </action>
  <verify>
    .venv\Scripts\python.exe -c "from models import AuditReport, ParsedHostname, AppStatus, CollectionResult; print('OK')"
    # Must print: OK

    .venv\Scripts\python.exe -c "
    from models import CollectionResult, ParsedHostname, AuditReport, AppStatus
    r = CollectionResult(value=1); assert r.ok
    r2 = CollectionResult(value=None, error='e'); assert not r2.ok
    ph = ParsedHostname(raw_hostname='PHX-INV-003')
    assert ph.raw_hostname == 'PHX-INV-003'
    assert ph.city is None
    assert ph.station is None
    ar = AuditReport(hostname='h', parsed_hostname=ph)
    ar2 = AuditReport(hostname='h2', parsed_hostname=ph)
    assert ar.apps is not ar2.apps
    print('All assertions passed')
    "
    # Must print: All assertions passed
  </verify>
  <acceptance_criteria>
    - `models.py` exists at project root
    - `grep -n "class AuditReport" models.py` returns a match
    - `grep -n "class ParsedHostname" models.py` returns a match
    - `grep -n "class AppStatus" models.py` returns a match
    - `grep -n "class CollectionResult" models.py` returns a match
    - `grep -n "parsed_hostname:" models.py` returns a match (NOT parsed_name)
    - `grep -n "department:" models.py` returns a match (NOT dept_code)
    - `grep -n "station: int" models.py` returns a match (NOT station: str)
    - `grep -n "default_factory=list" models.py` returns at least 3 matches (local_profiles, apps, collection_errors)
    - `grep -n "frozen=True" models.py` returns NO match
    - `.venv\Scripts\python.exe -c "from models import AuditReport, ParsedHostname, AppStatus, CollectionResult; print('OK')"` prints OK and exits 0
  </acceptance_criteria>
  <done>models.py exists at project root; all four classes importable; field names match ROADMAP SC1 and CONTEXT.md locked decisions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hostname string → ParsedHostname | Raw hostname input enters the data model here; must never be executed or passed to shell |
| External registry data → AppStatus | Phase 4 will populate AppStatus fields from registry reads; models.py defines the container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | ParsedHostname.raw_hostname | mitigate | raw_hostname is stored as a plain string and never passed to subprocess, eval, exec, or any shell. It is only displayed (rendered into HTML by Phase 3). HTML escaping is the renderer's responsibility. |
| T-01-02-02 | Information Disclosure | AuditReport (contains hostname, user profiles) | accept | AuditReport is never serialized to disk in Phase 1. Serialization risk is assessed in Phase 3 (HTML output) and Phase 5 (packaging). |
| T-01-02-03 | Elevation of Privilege | models.py import path | accept | models.py imports only stdlib (dataclasses, typing). No external code execution at import time. |
</threat_model>

<verification>
The following must all succeed after this plan:

  .venv\Scripts\python.exe -c "from models import AuditReport, ParsedHostname, AppStatus, CollectionResult; print('SC5 PASS')"
  # Satisfies ROADMAP Phase 1 Success Criterion 5
</verification>

<success_criteria>
- models.py exists at project root
- All four dataclasses (CollectionResult, ParsedHostname, AppStatus, AuditReport) are importable from models.py
- Field names match ROADMAP SC1: department (not dept_code), parsed_hostname (not parsed_name), station: int | None
- No mutable class-level defaults (all list fields use field(default_factory=list))
- No external dependencies imported (stdlib only: dataclasses, typing)
</success_criteria>

<output>
After completion, create `.planning/phases/01-models-and-hostname-parser/01-02-SUMMARY.md` following the summary template.
</output>
